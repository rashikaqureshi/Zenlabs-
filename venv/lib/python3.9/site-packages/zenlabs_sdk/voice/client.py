# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.api_key_create import ApiKeyCreate
from ..types.call_outcome_enum import CallOutcomeEnum
from ..types.direction_enum import DirectionEnum
from ..types.disposition_enum import DispositionEnum
from ..types.disposition_sentiment_enum import DispositionSentimentEnum
from ..types.event_type_enum import EventTypeEnum
from ..types.generate_recording_url_response import GenerateRecordingUrlResponse
from ..types.interaction_session import InteractionSession
from ..types.paginated_api_key_list_list import PaginatedApiKeyListList
from ..types.paginated_interaction_session_list import PaginatedInteractionSessionList
from ..types.paginated_phone_number_list import PaginatedPhoneNumberList
from ..types.paginated_transcriber_list import PaginatedTranscriberList
from ..types.paginated_transcript_post_list import PaginatedTranscriptPostList
from ..types.paginated_voice_provider_list import PaginatedVoiceProviderList
from ..types.paginated_voice_session_list import PaginatedVoiceSessionList
from ..types.patched_phone_number import PatchedPhoneNumber
from ..types.patched_transcriber import PatchedTranscriber
from ..types.patched_voice_provider import PatchedVoiceProvider
from ..types.phone_number import PhoneNumber
from ..types.session_token_response import SessionTokenResponse
from ..types.status9b1enum import Status9B1Enum
from ..types.text_src_enum import TextSrcEnum
from ..types.trace_analytics import TraceAnalytics
from ..types.trace_list_item import TraceListItem
from ..types.transcriber import Transcriber
from ..types.user import User
from ..types.voice_provider import VoiceProvider
from ..types.voice_session import VoiceSession
from ..types.workflow_configuration import WorkflowConfiguration
from .raw_client import AsyncRawVoiceClient, RawVoiceClient
from .types.voice_dashboard_sessions_list_request_direction import VoiceDashboardSessionsListRequestDirection
from .types.voice_dashboard_sessions_list_request_disposition import VoiceDashboardSessionsListRequestDisposition
from .types.voice_dashboard_sessions_list_request_disposition_sentiment import (
    VoiceDashboardSessionsListRequestDispositionSentiment,
)
from .types.voice_dashboard_sessions_list_request_status import VoiceDashboardSessionsListRequestStatus
from .types.voice_dashboard_sessions_transcripts_list_request_direction import (
    VoiceDashboardSessionsTranscriptsListRequestDirection,
)
from .types.voice_dashboard_sessions_transcripts_list_request_disposition import (
    VoiceDashboardSessionsTranscriptsListRequestDisposition,
)
from .types.voice_dashboard_sessions_transcripts_list_request_disposition_sentiment import (
    VoiceDashboardSessionsTranscriptsListRequestDispositionSentiment,
)
from .types.voice_dashboard_sessions_transcripts_list_request_status import (
    VoiceDashboardSessionsTranscriptsListRequestStatus,
)
from .types.voice_sessions_transcripts_create_response import VoiceSessionsTranscriptsCreateResponse
from .types.voice_sessions_transcripts_retrieve_response import VoiceSessionsTranscriptsRetrieveResponse
from .types.voice_sessions_variables_retrieve_response import VoiceSessionsVariablesRetrieveResponse
from .types.voice_workflow_config_retrieve_request_direction import VoiceWorkflowConfigRetrieveRequestDirection

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class VoiceClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawVoiceClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawVoiceClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawVoiceClient
        """
        return self._raw_client

    def amazon_connect_available_numbers_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        List phone numbers claimed on a specific Connect instance.

        Already-imported numbers are flagged by the upstream voice-py route; the
        browser renders those as disabled options without an extra round-trip.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.amazon_connect_available_numbers_retrieve()
        """
        _response = self._raw_client.amazon_connect_available_numbers_retrieve(request_options=request_options)
        return _response.data

    def amazon_connect_contact_flows_retrieve(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        List contact flows for a specific Connect instance.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.amazon_connect_contact_flows_retrieve()
        """
        _response = self._raw_client.amazon_connect_contact_flows_retrieve(request_options=request_options)
        return _response.data

    def amazon_connect_instances_retrieve(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        List Connect instances visible to a given Nango AWS IAM connection.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.amazon_connect_instances_retrieve()
        """
        _response = self._raw_client.amazon_connect_instances_retrieve(request_options=request_options)
        return _response.data

    def api_keys_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiKeyListList:
        """
        Base class mixin for model view sets. Implemented as a mixin to allow
        flexibility in the inheritance chain.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiKeyListList


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.api_keys_list()
        """
        _response = self._raw_client.api_keys_list(page=page, page_size=page_size, request_options=request_options)
        return _response.data

    def api_keys_create(
        self,
        *,
        name: str,
        id: typing.Optional[int] = OMIT,
        key: typing.Optional[str] = OMIT,
        prefix: typing.Optional[str] = OMIT,
        scopes: typing.Optional[typing.Sequence[str]] = OMIT,
        expires_at: typing.Optional[dt.datetime] = OMIT,
        created_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiKeyCreate:
        """
        Base class mixin for model view sets. Implemented as a mixin to allow
        flexibility in the inheritance chain.

        Parameters
        ----------
        name : str
            Human label (e.g. 'Production backend').

        id : typing.Optional[int]

        key : typing.Optional[str]

        prefix : typing.Optional[str]
            First 16 chars of the key (e.g. 'zn_live_a1b2c3d4'). For lookup and dashboard display.

        scopes : typing.Optional[typing.Sequence[str]]

        expires_at : typing.Optional[dt.datetime]
            Optional. NULL = never expires (until revoked).

        created_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiKeyCreate


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.api_keys_create(
            name="name",
        )
        """
        _response = self._raw_client.api_keys_create(
            name=name,
            id=id,
            key=key,
            prefix=prefix,
            scopes=scopes,
            expires_at=expires_at,
            created_at=created_at,
            request_options=request_options,
        )
        return _response.data

    def api_keys_destroy(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Base class mixin for model view sets. Implemented as a mixin to allow
        flexibility in the inheritance chain.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.api_keys_destroy(
            id="id",
        )
        """
        _response = self._raw_client.api_keys_destroy(id, request_options=request_options)
        return _response.data

    def checkpoint_create(
        self,
        *,
        session_id: int,
        node_name: str,
        extracted_data: typing.Any,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> VoiceSession:
        """
        Save extracted variables to the session checkpoint store and return updated state

        Parameters
        ----------
        session_id : int

        node_name : str
            Node name used to look up the node within the session workflow

        extracted_data : typing.Any

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSession


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.checkpoint_create(
            session_id=1,
            node_name="node_name",
            extracted_data={"key": "value"},
        )
        """
        _response = self._raw_client.checkpoint_create(
            session_id=session_id, node_name=node_name, extracted_data=extracted_data, request_options=request_options
        )
        return _response.data

    def dashboard_sessions_list(
        self,
        *,
        direction: typing.Optional[VoiceDashboardSessionsListRequestDirection] = None,
        disposition: typing.Optional[VoiceDashboardSessionsListRequestDisposition] = None,
        disposition_sentiment: typing.Optional[VoiceDashboardSessionsListRequestDispositionSentiment] = None,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        status: typing.Optional[VoiceDashboardSessionsListRequestStatus] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedInteractionSessionList:
        """
        List all Voice Sessions matching the applied filters

        Parameters
        ----------
        direction : typing.Optional[VoiceDashboardSessionsListRequestDirection]
            * `inbound` - Inbound
            * `outbound` - Outbound

        disposition : typing.Optional[VoiceDashboardSessionsListRequestDisposition]
            Computed interaction disposition category (ZEN-71).

            * `agent_escalation_immediate` - Agent Escalation Immediate
            * `agent_escalation_mid_call` - Agent Escalation Mid-Call
            * `hangup_immediate` - Hangup Immediate
            * `hangup_mid_call` - Hangup Mid-Call
            * `transfer_unsupported` - Transfer Unsupported
            * `resolved` - Resolved
            * `post_resolution_transfer` - Post Resolution Transfer
            * `fatal_error_escalated` - Fatal Error Escalated
            * `error` - Error

        disposition_sentiment : typing.Optional[VoiceDashboardSessionsListRequestDispositionSentiment]
            Sentiment derived from the disposition (ZEN-71).

            * `positive` - Positive
            * `neutral` - Neutral
            * `negative` - Negative

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        search : typing.Optional[str]
            A search term.

        status : typing.Optional[VoiceDashboardSessionsListRequestStatus]
            * `created` - Created
            * `queued` - Queued
            * `active` - Active
            * `completed` - Completed
            * `failed` - Failed
            * `cancelled` - Cancelled

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedInteractionSessionList


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.dashboard_sessions_list()
        """
        _response = self._raw_client.dashboard_sessions_list(
            direction=direction,
            disposition=disposition,
            disposition_sentiment=disposition_sentiment,
            page=page,
            page_size=page_size,
            search=search,
            status=status,
            request_options=request_options,
        )
        return _response.data

    def dashboard_sessions_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> InteractionSession:
        """
        Retrieve the specified Voice Session.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Interaction Session.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        InteractionSession


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.dashboard_sessions_retrieve(
            id=1,
        )
        """
        _response = self._raw_client.dashboard_sessions_retrieve(id, request_options=request_options)
        return _response.data

    def dashboard_sessions_transcripts_list(
        self,
        id: int,
        *,
        direction: typing.Optional[VoiceDashboardSessionsTranscriptsListRequestDirection] = None,
        disposition: typing.Optional[VoiceDashboardSessionsTranscriptsListRequestDisposition] = None,
        disposition_sentiment: typing.Optional[VoiceDashboardSessionsTranscriptsListRequestDispositionSentiment] = None,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        status: typing.Optional[VoiceDashboardSessionsTranscriptsListRequestStatus] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedTranscriptPostList:
        """
        Returns transcript posts for a session's conversation. Admin users (is_staff) see all rows including rich step/variable events; non-admin users see only legacy message rows.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Interaction Session.

        direction : typing.Optional[VoiceDashboardSessionsTranscriptsListRequestDirection]
            * `inbound` - Inbound
            * `outbound` - Outbound

        disposition : typing.Optional[VoiceDashboardSessionsTranscriptsListRequestDisposition]
            Computed interaction disposition category (ZEN-71).

            * `agent_escalation_immediate` - Agent Escalation Immediate
            * `agent_escalation_mid_call` - Agent Escalation Mid-Call
            * `hangup_immediate` - Hangup Immediate
            * `hangup_mid_call` - Hangup Mid-Call
            * `transfer_unsupported` - Transfer Unsupported
            * `resolved` - Resolved
            * `post_resolution_transfer` - Post Resolution Transfer
            * `fatal_error_escalated` - Fatal Error Escalated
            * `error` - Error

        disposition_sentiment : typing.Optional[VoiceDashboardSessionsTranscriptsListRequestDispositionSentiment]
            Sentiment derived from the disposition (ZEN-71).

            * `positive` - Positive
            * `neutral` - Neutral
            * `negative` - Negative

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        search : typing.Optional[str]
            A search term.

        status : typing.Optional[VoiceDashboardSessionsTranscriptsListRequestStatus]
            * `created` - Created
            * `queued` - Queued
            * `active` - Active
            * `completed` - Completed
            * `failed` - Failed
            * `cancelled` - Cancelled

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedTranscriptPostList


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.dashboard_sessions_transcripts_list(
            id=1,
        )
        """
        _response = self._raw_client.dashboard_sessions_transcripts_list(
            id,
            direction=direction,
            disposition=disposition,
            disposition_sentiment=disposition_sentiment,
            page=page,
            page_size=page_size,
            search=search,
            status=status,
            request_options=request_options,
        )
        return _response.data

    def integration_connections_aws_auth_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Resolve AWS IAM credentials by ``IntegrationConnection`` pk.

        Service-token-only. Used by voice-py's import-drawer read endpoints
        (Phase 12.5) which list Connect instances, contact flows, and phone
        numbers *before* any ``AwsConnectPhoneNumber`` row exists.

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.integration_connections_aws_auth_retrieve(
            id=1,
        )
        """
        _response = self._raw_client.integration_connections_aws_auth_retrieve(id, request_options=request_options)
        return _response.data

    def internal_sessions_retry_summary_create(
        self, session_id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        ``POST /api/v1/voice/internal/sessions/<int:session_id>/retry-summary/``

        Service-token authenticated. Returns 200 with ``persisted: false`` in
        the ack-only phase; the field flips to ``true`` once the in-place
        regeneration lands.

        Parameters
        ----------
        session_id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.internal_sessions_retry_summary_create(
            session_id=1,
        )
        """
        _response = self._raw_client.internal_sessions_retry_summary_create(session_id, request_options=request_options)
        return _response.data

    def langsmith_analytics_list(
        self,
        *,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[TraceListItem]:
        """
        List recent conversation traces with summary metrics.

        Parameters
        ----------
        limit : typing.Optional[int]
            Number of traces to return (default 20, max 100)

        offset : typing.Optional[int]
            Offset for pagination (default 0)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[TraceListItem]


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.langsmith_analytics_list()
        """
        _response = self._raw_client.langsmith_analytics_list(
            limit=limit, offset=offset, request_options=request_options
        )
        return _response.data

    def langsmith_analytics_analytics_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> TraceAnalytics:
        """
        Fetch detailed analytics for a specific LangSmith trace, including cost, latency, token usage, and per-turn breakdown.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        TraceAnalytics


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.langsmith_analytics_analytics_retrieve(
            id="id",
        )
        """
        _response = self._raw_client.langsmith_analytics_analytics_retrieve(id, request_options=request_options)
        return _response.data

    def list_voices_retrieve(
        self, *, workflow_id: int, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Return available voices from the workflow's configured TTS provider. Results are cached for 5 minutes per provider instance.

        Parameters
        ----------
        workflow_id : int
            Workflow ID whose voice provider to query.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.list_voices_retrieve(
            workflow_id=1,
        )
        """
        _response = self._raw_client.list_voices_retrieve(workflow_id=workflow_id, request_options=request_options)
        return _response.data

    def outbound_correlations_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        ``POST /api/v1/voice/outbound-correlations/``

        Records a new correlation row at AWS dispatch time. Idempotent on
        both ``aws_contact_id`` (AWS retry double-billing) and
        ``integration_workflow_id`` (Temporal retry that produced different
        AWS ContactIds for the same logical dial).

        Request body::

            {
                "aws_contact_id": "<connect-contact-id>",
                "source_phone": "<E.164>",
                "destination_phone": "<E.164>",
                "integration_workflow_id": "<temporal-wf-id>",
                "phone_number_id": <int>,
                "tenant_id": <int>
            }

        Responses:
            201 ``{"id": <int>}`` — new row inserted
            200 ``{"id": <int>, "duplicate": "<aws_contact_id|integration_workflow_id>"}`` — idempotent hit
            400 — validation error
            401 — invalid/missing service token

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.outbound_correlations_create()
        """
        _response = self._raw_client.outbound_correlations_create(request_options=request_options)
        return _response.data

    def outbound_correlations_abandon_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        ``POST /api/v1/voice/outbound-correlations/abandon/``

        Marks a correlation row as abandoned so future claims for the same
        ``(source_phone, destination_phone)`` skip it. Called by
        integrations-py via voice-py at ``OutboundCallWorkflow`` exit when
        no Twilio CallSid was bound to the workflow.

        Race precedence: claim wins. The atomic UPDATE uses
        ``WHERE claimed_at IS NULL AND abandoned_at IS NULL`` so an
        in-flight claim that committed between the workflow's exit
        decision and this request lands first; the abandon then no-ops
        and we return ``{"abandoned": false, "reason": "already_claimed"}``.

        Best-effort: failures here don't block the workflow. The retention
        sweeper (3-day TTL) is the durable safety net — abandon is purely
        opportunistic cleanup that releases the (source, destination) slot
        sooner than ``claim_expires_at`` (2 min) would.

        Tenant scoping: integration_workflow_id is globally unique, so we
        don't require a tenant_id parameter — the row's tenant is implicit
        in the workflow id. (Same pattern as record_outbound_correlation's
        tenant inference from phone_number_id.)

        Request body::

            {
                "integration_workflow_id": "<temporal-wf-id>",
                "reason": "<short_tag>",  # optional, defaults to ""
            }

        Responses:
            200 ``{"abandoned": true,  "id": <int>}`` — row marked
            200 ``{"abandoned": false, "reason": "already_claimed"}``
            200 ``{"abandoned": false, "reason": "not_found"}``
            200 ``{"abandoned": false, "reason": "already_abandoned"}``
            400 — validation error
            401 — invalid/missing service token

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.outbound_correlations_abandon_create()
        """
        _response = self._raw_client.outbound_correlations_abandon_create(request_options=request_options)
        return _response.data

    def outbound_correlations_by_call_sid_retrieve(
        self, call_sid: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        ``GET /api/v1/voice/outbound-correlations/by-call-sid/<call_sid>/?tenant_id=<int>``

        Look up an already-claimed correlation row by its Twilio CallSid so
        the voice-py status-callback handler can forward
        ``on_call_status`` to the integrations-py ``OutboundCallWorkflow``.
        Without this signal the workflow blocks on ``wait_condition`` until
        its ``run_timeout_seconds`` elapses and resolves as ``timeout``,
        even when ``on_call_enrichment`` has already arrived.

        Service-token auth + **mandatory tenant scoping** via the
        ``tenant_id`` query param. CallSids are not secret (they appear in
        Twilio console, status callbacks, and logs), so a global lookup
        would let any service-token holder probe known CallSids and
        enumerate correlations across tenants. The caller (voice-py) has
        ``tenant_id`` from the cached session row, so the requirement is
        free at the call site.

        Responses:
            200 ``{"integration_workflow_id": <str>, "tenant_id": <int>}``
            404 — no row claimed under that call_sid for the given tenant
            400 — missing/non-integer tenant_id query param
            401 — invalid/missing service token

        Parameters
        ----------
        call_sid : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.outbound_correlations_by_call_sid_retrieve(
            call_sid="call_sid",
        )
        """
        _response = self._raw_client.outbound_correlations_by_call_sid_retrieve(
            call_sid, request_options=request_options
        )
        return _response.data

    def outbound_correlations_claim_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        ``POST /api/v1/voice/outbound-correlations/claim/``

        Atomically claims the oldest unclaimed correlation matching
        ``(source_phone, destination_phone)`` whose ``claim_expires_at`` is
        still in the future (Phase 14: column-driven 2-min TTL replaces
        the legacy 180s ``created_at``-derived window).

        Reconnect path: if a row with this ``twilio_call_sid`` already
        exists (Twilio webhook retry on the same call), return its
        ``integration_workflow_id`` directly without re-claiming. The
        reconnect lookup is intentionally NOT gated on
        ``claim_expires_at`` / ``abandoned_at`` — once a row is claimed
        its workflow id is the durable answer for that CallSid until
        retention expires, regardless of TTL/abandon state.

        Single-statement claim: ``UPDATE ... WHERE id=(SELECT ... FOR
        UPDATE SKIP LOCKED LIMIT 1) RETURNING ...``. Survives Supabase
        transaction-mode pooling because it's one round-trip; no FOR UPDATE
        held across statements.

        Request body::

            {
                "source_phone": "<Twilio.To, E.164>",
                "destination_phone": "<Twilio.From, E.164>",
                "twilio_call_sid": "<CA...>",
                "tenant_id": <int>
            }

        Responses:
            200 ``{"integration_workflow_id": "<id>"}`` — hit
            204 — no eligible row in the claim window
            401 — invalid/missing service token

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.outbound_correlations_claim_create()
        """
        _response = self._raw_client.outbound_correlations_claim_create(request_options=request_options)
        return _response.data

    def outbound_correlations_sweep_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        ``POST /api/v1/voice/outbound-correlations/sweep/``

        Phase 14: deletes correlation rows past their
        ``retention_expires_at`` deadline. Service-token only — designed
        to be invoked by a Temporal scheduled workflow (see
        ``ScheduledTriggerWorkflow``) on a daily cadence at low traffic.

        Cross-tenant by design (the sweeper has no tenant context and
        retention is uniform across tenants); ``disable_auto_filters``
        bypasses the tenant ContextVar manager filter so all expired
        rows surface for the same DELETE.

        The implementation mirrors ``reap_outbound_correlations`` so an
        operator can either run the management command on a host (e.g.
        one-shot ad-hoc cleanup) OR rely on the Temporal-driven HTTP
        path for production. Both paths converge on the same
        ``retention_expires_at < now()`` predicate.

        Hard upper bound: ``MAX_DELETES_PER_REQUEST`` caps the total number
        of rows a single sweep call may delete (defense-in-depth against a
        misconfigured schedule firing every minute or a token-compromise
        blast). Operators can re-invoke the endpoint or run the
        ``reap_outbound_correlations`` management command for unbounded
        one-shot cleanups.

        Request body: ``{"batch_size": <int, default=1000>}`` (optional)
        Response: 200 ``{"deleted": <int>, "cutoff": "<ISO>", "capped": <bool>}``

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.outbound_correlations_sweep_create()
        """
        _response = self._raw_client.outbound_correlations_sweep_create(request_options=request_options)
        return _response.data

    def phone_numbers_list(
        self,
        *,
        inbound_workflow: typing.Optional[int] = None,
        outbound_workflow: typing.Optional[int] = None,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPhoneNumberList:
        """
        List all Twilio Phone Numbers matching the applied filters

        Parameters
        ----------
        inbound_workflow : typing.Optional[int]

        outbound_workflow : typing.Optional[int]

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        search : typing.Optional[str]
            A search term.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPhoneNumberList


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.phone_numbers_list()
        """
        _response = self._raw_client.phone_numbers_list(
            inbound_workflow=inbound_workflow,
            outbound_workflow=outbound_workflow,
            page=page,
            page_size=page_size,
            search=search,
            request_options=request_options,
        )
        return _response.data

    def phone_numbers_create(
        self, *, request: PhoneNumber, request_options: typing.Optional[RequestOptions] = None
    ) -> PhoneNumber:
        """
        Create a new Twilio Phone Number.

        Parameters
        ----------
        request : PhoneNumber

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PhoneNumber


        Examples
        --------
        from zenlabs_sdk import PhoneNumber_TwilioPhoneNumber, ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.phone_numbers_create(
            request=PhoneNumber_TwilioPhoneNumber(
                phone_number="phone_number",
            ),
        )
        """
        _response = self._raw_client.phone_numbers_create(request=request, request_options=request_options)
        return _response.data

    def phone_numbers_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PhoneNumber:
        """
        Retrieve the specified Twilio Phone Number.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Phone Number.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PhoneNumber


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.phone_numbers_retrieve(
            id=1,
        )
        """
        _response = self._raw_client.phone_numbers_retrieve(id, request_options=request_options)
        return _response.data

    def phone_numbers_update(
        self, id: int, *, request: PhoneNumber, request_options: typing.Optional[RequestOptions] = None
    ) -> PhoneNumber:
        """
        Update the specified Twilio Phone Number.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Phone Number.

        request : PhoneNumber

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PhoneNumber


        Examples
        --------
        from zenlabs_sdk import PhoneNumber_TwilioPhoneNumber, ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.phone_numbers_update(
            id=1,
            request=PhoneNumber_TwilioPhoneNumber(
                phone_number="phone_number",
            ),
        )
        """
        _response = self._raw_client.phone_numbers_update(id, request=request, request_options=request_options)
        return _response.data

    def phone_numbers_destroy(self, id: int, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete the specified Twilio Phone Number. This may archive/delete related objects.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Phone Number.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.phone_numbers_destroy(
            id=1,
        )
        """
        _response = self._raw_client.phone_numbers_destroy(id, request_options=request_options)
        return _response.data

    def phone_numbers_partial_update(
        self, id: int, *, request: PatchedPhoneNumber, request_options: typing.Optional[RequestOptions] = None
    ) -> PhoneNumber:
        """
        Update the specified Twilio Phone Number.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Phone Number.

        request : PatchedPhoneNumber

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PhoneNumber


        Examples
        --------
        from zenlabs_sdk import PatchedPhoneNumber_TwilioPhoneNumber, ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.phone_numbers_partial_update(
            id=1,
            request=PatchedPhoneNumber_TwilioPhoneNumber(),
        )
        """
        _response = self._raw_client.phone_numbers_partial_update(id, request=request, request_options=request_options)
        return _response.data

    def phone_numbers_auth_retrieve(self, id: int, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Return Twilio credentials for a phone number (service-token only).

        Used by the voice service to fetch Twilio credentials at activity
        runtime rather than storing them in Temporal workflow history.

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.phone_numbers_auth_retrieve(
            id=1,
        )
        """
        _response = self._raw_client.phone_numbers_auth_retrieve(id, request_options=request_options)
        return _response.data

    def phone_numbers_config_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Return non-credential phone number config (iPaaS service-token only).

        Used by the iPaaS pre-call pipeline to derive ``from_phone`` and the
        default outbound workflow id from a ``phone_number_id``, so schedules
        only need ``phone_number_id`` instead of duplicating these fields.

        Distinct from ``PhoneNumberAuthView`` (which exposes Twilio credentials
        to voice-py via ``ZENAPI_VOICE_SERVICE_TOKEN``) — this endpoint:
        - NEVER returns auth_token/account_sid
        - Authenticates via ``ZENAPI_INTEGRATIONS_SERVICE_TOKEN`` (iPaaS identity)

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.phone_numbers_config_retrieve(
            id=1,
        )
        """
        _response = self._raw_client.phone_numbers_config_retrieve(id, request_options=request_options)
        return _response.data

    def phone_numbers_resync_webhooks_create(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Re-attempt Twilio webhook configuration for an existing phone.

        Surfaces the failure reason via a non-2xx so the UI can flash the
        exact Twilio/Nango message (instead of silently retrying).

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Phone Number.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.phone_numbers_resync_webhooks_create(
            id=1,
        )
        """
        _response = self._raw_client.phone_numbers_resync_webhooks_create(id, request_options=request_options)
        return _response.data

    def phone_numbers_by_phone_retrieve(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        ``GET /api/v1/voice/phone-numbers/by-phone/?phone=<E.164>``

        Lightweight service-token lookup that resolves an E.164 phone string
        to the tenant + ``phone_number_id`` of an ``AwsConnectPhoneNumber``
        row that owns it. Matches on either ``bridge_phone`` (the Twilio
        BYOC bridge number AWS Connect dials) OR ``phone_number`` (the AWS
        caller-ID for single-number deploys).

        Used by voice-py's Twilio BYOC inbound webhook (ZEN-205) to get
        tenant context BEFORE creating a session, so it can claim the
        outbound correlation and ``attach_call_sid`` to the pre-existing
        dispatch session — the bridge that makes per-call ``variables``
        reach the bot for AWS-Connect-originated calls.

        Non-AWS phones return 404. Voice-py treats 404 as "this isn't a
        BYOC bridge inbound, fall through to legacy session-creation
        path." That preserves regular Twilio inbound behavior.

        Phone numbers are globally unique (``BasePhoneNumber`` has a
        unique constraint on ``phone_number`` for ``is_deleted=False``),
        so at most one row matches per query — no cross-tenant
        disambiguation needed.

        Responses:
            200 ``{"tenant_id": <int>, "phone_number_id": <int>, "provider": "amazon_connect"}``
            404 — no AwsConnectPhoneNumber owns this number
            400 — missing/empty ``phone`` query param
            401 — invalid/missing service token

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.phone_numbers_by_phone_retrieve()
        """
        _response = self._raw_client.phone_numbers_by_phone_retrieve(request_options=request_options)
        return _response.data

    def preview_voice_create(
        self,
        *,
        workflow_id: int,
        text: typing.Optional[str] = OMIT,
        voice_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Stream a short TTS audio sample using the workflow's configured voice provider. The API key is resolved from the workspace secret or the provider's encrypted key — never sent from the client.

        Parameters
        ----------
        workflow_id : int
            Workflow ID whose voice provider to preview.

        text : typing.Optional[str]
            Text to synthesize. Defaults to a short greeting.

        voice_id : typing.Optional[str]
            Override voice ID (e.g. when previewing a preset before saving).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.preview_voice_create(
            workflow_id=1,
        )
        """
        _response = self._raw_client.preview_voice_create(
            workflow_id=workflow_id, text=text, voice_id=voice_id, request_options=request_options
        )
        return _response.data

    def provider_metadata_retrieve(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Return the catalog of voice provider + transcriber enum values.

        Powers UI dropdowns for model selection, emotion picking, language
        selection, etc. without forcing the frontend to mirror Python enums.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.provider_metadata_retrieve()
        """
        _response = self._raw_client.provider_metadata_retrieve(request_options=request_options)
        return _response.data

    def secrets_resolve_retrieve(self, id: int, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Service-token endpoint that returns the plaintext secret value.

        Consumed by voice-py at call setup. Knox-token auth is rejected — this
        path bypasses the user-facing tenant-scoped manager and looks up the
        secret across tenants by id, so we tightly gate it on ``ServiceToken``.

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.secrets_resolve_retrieve(
            id=1,
        )
        """
        _response = self._raw_client.secrets_resolve_retrieve(id, request_options=request_options)
        return _response.data

    def session_token_create(
        self, *, workflow_id: int, request_options: typing.Optional[RequestOptions] = None
    ) -> SessionTokenResponse:
        """
        Parameters
        ----------
        workflow_id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SessionTokenResponse


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.session_token_create(
            workflow_id=1,
        )
        """
        _response = self._raw_client.session_token_create(workflow_id=workflow_id, request_options=request_options)
        return _response.data

    def sessions_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedVoiceSessionList:
        """
        List all Voice Sessions matching the applied filters

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedVoiceSessionList


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.sessions_list()
        """
        _response = self._raw_client.sessions_list(page=page, page_size=page_size, request_options=request_options)
        return _response.data

    def sessions_create(
        self,
        *,
        from_number: str,
        to_number: str,
        direction: DirectionEnum,
        provider_ref_id: typing.Optional[str] = OMIT,
        workflow_id: typing.Optional[int] = OMIT,
        variables: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> VoiceSession:
        """
        Create a new Voice Session.

        Parameters
        ----------
        from_number : str
            E.164 formatted from phone number

        to_number : str
            E.164 formatted to phone number

        direction : DirectionEnum
            Call direction (inbound or outbound)

            * `inbound` - Inbound
            * `outbound` - Outbound

        provider_ref_id : typing.Optional[str]
            The Call SID or external ID from the provider

        workflow_id : typing.Optional[int]
            Workflow ID for browser sessions (bypasses phone number lookup)

        variables : typing.Optional[typing.Any]
            Campaign contact variables passed to the agent

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSession


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.sessions_create(
            from_number="from_number",
            to_number="to_number",
            direction="inbound",
        )
        """
        _response = self._raw_client.sessions_create(
            from_number=from_number,
            to_number=to_number,
            direction=direction,
            provider_ref_id=provider_ref_id,
            workflow_id=workflow_id,
            variables=variables,
            request_options=request_options,
        )
        return _response.data

    def sessions_retrieve(self, id: int, *, request_options: typing.Optional[RequestOptions] = None) -> VoiceSession:
        """
        Retrieve the specified Voice Session.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSession


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.sessions_retrieve(
            id=1,
        )
        """
        _response = self._raw_client.sessions_retrieve(id, request_options=request_options)
        return _response.data

    def sessions_update(
        self,
        id_: int,
        *,
        from_number: str,
        to_number: str,
        id: typing.Optional[int] = OMIT,
        tenant_id: typing.Optional[int] = OMIT,
        conversation_id: typing.Optional[int] = OMIT,
        active_agent_id: typing.Optional[int] = OMIT,
        status: typing.Optional[Status9B1Enum] = OMIT,
        direction: typing.Optional[DirectionEnum] = OMIT,
        call_outcome: typing.Optional[CallOutcomeEnum] = OMIT,
        provider_ref_id: typing.Optional[str] = OMIT,
        duration_seconds: typing.Optional[int] = OMIT,
        recording_url: typing.Optional[str] = OMIT,
        provider_status: typing.Optional[str] = OMIT,
        variables: typing.Optional[typing.Any] = OMIT,
        summary: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        collected_variables: typing.Optional[typing.Any] = OMIT,
        summary_pending: typing.Optional[bool] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        ended_at: typing.Optional[dt.datetime] = OMIT,
        end_reason: typing.Optional[str] = OMIT,
        disposition: typing.Optional[DispositionEnum] = OMIT,
        disposition_sentiment: typing.Optional[DispositionSentimentEnum] = OMIT,
        user_turn_count: typing.Optional[int] = OMIT,
        created_at: typing.Optional[dt.datetime] = OMIT,
        updated_at: typing.Optional[dt.datetime] = OMIT,
        created_by: typing.Optional[User] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> VoiceSession:
        """
        Update the specified Voice Session.

        Parameters
        ----------
        id_ : int
            A unique integer value identifying this Voice Session.

        from_number : str
            E.164 formatted number

        to_number : str
            E.164 formatted number

        id : typing.Optional[int]

        tenant_id : typing.Optional[int]

        conversation_id : typing.Optional[int]
            The transcript associated with this interaction.

        active_agent_id : typing.Optional[int]
            The specific workflow handling this interaction.

        status : typing.Optional[Status9B1Enum]

        direction : typing.Optional[DirectionEnum]

        call_outcome : typing.Optional[CallOutcomeEnum]
            The telephony disposition.

            * `unknown` - Unknown
            * `answered` - Answered
            * `busy` - Busy
            * `no_answer` - No Answer
            * `failed` - Failed
            * `canceled` - Canceled
            * `voicemail` - Voicemail

        provider_ref_id : typing.Optional[str]
            The Call SID or external ID from the provider.

        duration_seconds : typing.Optional[int]
            Duration of the call in seconds

        recording_url : typing.Optional[str]
            Public or signed URL to the call audio.

        provider_status : typing.Optional[str]
            Raw status returned by the telephony provider.

        variables : typing.Optional[typing.Any]
            Campaign contact variables passed to the agent.

        summary : typing.Optional[str]
            LLM-generated one-sentence call summary.

        description : typing.Optional[str]
            LLM-generated 2-3 sentence call description.

        collected_variables : typing.Optional[typing.Any]
            LLM-extracted variables collected during the call (distinct from inbound campaign `variables`).

        summary_pending : typing.Optional[bool]
            True while the post-call summary pipeline owes this session a summary. Cleared on success or skip; the reconciler retries any row stuck True past the grace window.

        started_at : typing.Optional[dt.datetime]

        ended_at : typing.Optional[dt.datetime]

        end_reason : typing.Optional[str]

        disposition : typing.Optional[DispositionEnum]
            Computed interaction disposition category (ZEN-71).

            * `agent_escalation_immediate` - Agent Escalation Immediate
            * `agent_escalation_mid_call` - Agent Escalation Mid-Call
            * `hangup_immediate` - Hangup Immediate
            * `hangup_mid_call` - Hangup Mid-Call
            * `transfer_unsupported` - Transfer Unsupported
            * `resolved` - Resolved
            * `post_resolution_transfer` - Post Resolution Transfer
            * `fatal_error_escalated` - Fatal Error Escalated
            * `error` - Error

        disposition_sentiment : typing.Optional[DispositionSentimentEnum]
            Sentiment derived from the disposition (ZEN-71).

            * `positive` - Positive
            * `neutral` - Neutral
            * `negative` - Negative

        user_turn_count : typing.Optional[int]
            Number of user turns observed during the interaction. Reported by the runtime alongside disposition for analytics breakdowns (ZEN-71).

        created_at : typing.Optional[dt.datetime]

        updated_at : typing.Optional[dt.datetime]

        created_by : typing.Optional[User]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSession


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.sessions_update(
            id_=1,
            from_number="from_number",
            to_number="to_number",
        )
        """
        _response = self._raw_client.sessions_update(
            id_,
            from_number=from_number,
            to_number=to_number,
            id=id,
            tenant_id=tenant_id,
            conversation_id=conversation_id,
            active_agent_id=active_agent_id,
            status=status,
            direction=direction,
            call_outcome=call_outcome,
            provider_ref_id=provider_ref_id,
            duration_seconds=duration_seconds,
            recording_url=recording_url,
            provider_status=provider_status,
            variables=variables,
            summary=summary,
            description=description,
            collected_variables=collected_variables,
            summary_pending=summary_pending,
            started_at=started_at,
            ended_at=ended_at,
            end_reason=end_reason,
            disposition=disposition,
            disposition_sentiment=disposition_sentiment,
            user_turn_count=user_turn_count,
            created_at=created_at,
            updated_at=updated_at,
            created_by=created_by,
            request_options=request_options,
        )
        return _response.data

    def sessions_destroy(self, id: int, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete the specified Voice Session. This may archive/delete related objects.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.sessions_destroy(
            id=1,
        )
        """
        _response = self._raw_client.sessions_destroy(id, request_options=request_options)
        return _response.data

    def sessions_partial_update(
        self,
        id: int,
        *,
        status: typing.Optional[Status9B1Enum] = OMIT,
        direction: typing.Optional[DirectionEnum] = OMIT,
        call_outcome: typing.Optional[CallOutcomeEnum] = OMIT,
        provider_ref_id: typing.Optional[str] = OMIT,
        from_number: typing.Optional[str] = OMIT,
        to_number: typing.Optional[str] = OMIT,
        duration_seconds: typing.Optional[int] = OMIT,
        recording_url: typing.Optional[str] = OMIT,
        provider_status: typing.Optional[str] = OMIT,
        variables: typing.Optional[typing.Any] = OMIT,
        summary: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        collected_variables: typing.Optional[typing.Any] = OMIT,
        summary_pending: typing.Optional[bool] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        ended_at: typing.Optional[dt.datetime] = OMIT,
        end_reason: typing.Optional[str] = OMIT,
        disposition: typing.Optional[DispositionEnum] = OMIT,
        disposition_sentiment: typing.Optional[DispositionSentimentEnum] = OMIT,
        user_turn_count: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> VoiceSession:
        """
        Update the specified Voice Session.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        status : typing.Optional[Status9B1Enum]

        direction : typing.Optional[DirectionEnum]

        call_outcome : typing.Optional[CallOutcomeEnum]
            The telephony disposition.

            * `unknown` - Unknown
            * `answered` - Answered
            * `busy` - Busy
            * `no_answer` - No Answer
            * `failed` - Failed
            * `canceled` - Canceled
            * `voicemail` - Voicemail

        provider_ref_id : typing.Optional[str]
            The Call SID or external ID from the provider.

        from_number : typing.Optional[str]
            E.164 formatted number

        to_number : typing.Optional[str]
            E.164 formatted number

        duration_seconds : typing.Optional[int]
            Duration of the call in seconds

        recording_url : typing.Optional[str]
            Public or signed URL to the call audio.

        provider_status : typing.Optional[str]
            Raw status returned by the telephony provider.

        variables : typing.Optional[typing.Any]
            Campaign contact variables passed to the agent.

        summary : typing.Optional[str]
            LLM-generated one-sentence call summary.

        description : typing.Optional[str]
            LLM-generated 2-3 sentence call description.

        collected_variables : typing.Optional[typing.Any]
            LLM-extracted variables collected during the call (distinct from inbound campaign `variables`).

        summary_pending : typing.Optional[bool]
            True while the post-call summary pipeline owes this session a summary. Cleared on success or skip; the reconciler retries any row stuck True past the grace window.

        started_at : typing.Optional[dt.datetime]

        ended_at : typing.Optional[dt.datetime]

        end_reason : typing.Optional[str]

        disposition : typing.Optional[DispositionEnum]
            Computed interaction disposition category (ZEN-71).

            * `agent_escalation_immediate` - Agent Escalation Immediate
            * `agent_escalation_mid_call` - Agent Escalation Mid-Call
            * `hangup_immediate` - Hangup Immediate
            * `hangup_mid_call` - Hangup Mid-Call
            * `transfer_unsupported` - Transfer Unsupported
            * `resolved` - Resolved
            * `post_resolution_transfer` - Post Resolution Transfer
            * `fatal_error_escalated` - Fatal Error Escalated
            * `error` - Error

        disposition_sentiment : typing.Optional[DispositionSentimentEnum]
            Sentiment derived from the disposition (ZEN-71).

            * `positive` - Positive
            * `neutral` - Neutral
            * `negative` - Negative

        user_turn_count : typing.Optional[int]
            Number of user turns observed during the interaction. Reported by the runtime alongside disposition for analytics breakdowns (ZEN-71).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSession


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.sessions_partial_update(
            id=1,
        )
        """
        _response = self._raw_client.sessions_partial_update(
            id,
            status=status,
            direction=direction,
            call_outcome=call_outcome,
            provider_ref_id=provider_ref_id,
            from_number=from_number,
            to_number=to_number,
            duration_seconds=duration_seconds,
            recording_url=recording_url,
            provider_status=provider_status,
            variables=variables,
            summary=summary,
            description=description,
            collected_variables=collected_variables,
            summary_pending=summary_pending,
            started_at=started_at,
            ended_at=ended_at,
            end_reason=end_reason,
            disposition=disposition,
            disposition_sentiment=disposition_sentiment,
            user_turn_count=user_turn_count,
            request_options=request_options,
        )
        return _response.data

    def sessions_confirm_upload_create(
        self, id: int, *, file_path: str, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceSession:
        """
        Confirm the recording upload and save the final S3 URL to the session

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        file_path : str
            The S3 file path of the uploaded recording

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSession


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.sessions_confirm_upload_create(
            id=1,
            file_path="file_path",
        )
        """
        _response = self._raw_client.sessions_confirm_upload_create(
            id, file_path=file_path, request_options=request_options
        )
        return _response.data

    def sessions_recording_url_create(
        self,
        id: int,
        *,
        file_extension: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GenerateRecordingUrlResponse:
        """
        Generate a presigned URL for uploading voice recording to S3

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        file_extension : typing.Optional[str]
            File extension for the recording (default: wav)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GenerateRecordingUrlResponse


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.sessions_recording_url_create(
            id=1,
        )
        """
        _response = self._raw_client.sessions_recording_url_create(
            id, file_extension=file_extension, request_options=request_options
        )
        return _response.data

    def sessions_status_partial_update(
        self,
        id: int,
        *,
        status: typing.Optional[Status9B1Enum] = OMIT,
        call_outcome: typing.Optional[CallOutcomeEnum] = OMIT,
        provider_status: typing.Optional[str] = OMIT,
        duration_seconds: typing.Optional[int] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        ended_at: typing.Optional[dt.datetime] = OMIT,
        end_reason: typing.Optional[str] = OMIT,
        disposition: typing.Optional[DispositionEnum] = OMIT,
        user_turn_count: typing.Optional[int] = OMIT,
        summary_pending: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> VoiceSession:
        """
        Update the call status and related fields throughout the call process

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        status : typing.Optional[Status9B1Enum]
            Session status

            * `created` - Created
            * `queued` - Queued
            * `active` - Active
            * `completed` - Completed
            * `failed` - Failed
            * `cancelled` - Cancelled

        call_outcome : typing.Optional[CallOutcomeEnum]
            Call outcome

            * `unknown` - Unknown
            * `answered` - Answered
            * `busy` - Busy
            * `no_answer` - No Answer
            * `failed` - Failed
            * `canceled` - Canceled
            * `voicemail` - Voicemail

        provider_status : typing.Optional[str]
            Raw status from provider

        duration_seconds : typing.Optional[int]
            Duration in seconds

        started_at : typing.Optional[dt.datetime]
            Start time

        ended_at : typing.Optional[dt.datetime]
            End time

        end_reason : typing.Optional[str]
            Reason for call ending

        disposition : typing.Optional[DispositionEnum]
            Computed call disposition category (ZEN-71). The matching disposition_sentiment is derived server-side — clients should not send it.

            * `agent_escalation_immediate` - Agent Escalation Immediate
            * `agent_escalation_mid_call` - Agent Escalation Mid-Call
            * `hangup_immediate` - Hangup Immediate
            * `hangup_mid_call` - Hangup Mid-Call
            * `transfer_unsupported` - Transfer Unsupported
            * `resolved` - Resolved
            * `post_resolution_transfer` - Post Resolution Transfer
            * `fatal_error_escalated` - Fatal Error Escalated
            * `error` - Error

        user_turn_count : typing.Optional[int]
            Number of user turns observed during the call (ZEN-71).

        summary_pending : typing.Optional[bool]
            Reliability marker for the post-call summary pipeline (architect H7). Voice-py sets True alongside the terminal status update — BEFORE the inline summary path starts — so an OOM-kill between the status update and the summary path still leaves a breadcrumb the reconciler can sweep. Cleared on successful summary persist.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSession


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.sessions_status_partial_update(
            id=1,
        )
        """
        _response = self._raw_client.sessions_status_partial_update(
            id,
            status=status,
            call_outcome=call_outcome,
            provider_status=provider_status,
            duration_seconds=duration_seconds,
            started_at=started_at,
            ended_at=ended_at,
            end_reason=end_reason,
            disposition=disposition,
            user_turn_count=user_turn_count,
            summary_pending=summary_pending,
            request_options=request_options,
        )
        return _response.data

    def sessions_transcripts_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceSessionsTranscriptsRetrieveResponse:
        """
        Add a single transcript post to the session's conversation

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSessionsTranscriptsRetrieveResponse


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.sessions_transcripts_retrieve(
            id=1,
        )
        """
        _response = self._raw_client.sessions_transcripts_retrieve(id, request_options=request_options)
        return _response.data

    def sessions_transcripts_create(
        self,
        id: int,
        *,
        text_src: TextSrcEnum,
        text: typing.Optional[str] = OMIT,
        timestamp: typing.Optional[dt.datetime] = OMIT,
        event_type: typing.Optional[EventTypeEnum] = OMIT,
        metadata: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> VoiceSessionsTranscriptsCreateResponse:
        """
        Add a single transcript post to the session's conversation

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        text_src : TextSrcEnum
            Source of the text (User, Assistant, Tool, Step, Variable). Use Step/Variable for rich events.

            * `User` - User
            * `Assistant` - Assistant
            * `Tool` - Tool
            * `Step` - Step
            * `Variable` - Variable

        text : typing.Optional[str]
            The transcript text content. Required for event_type='message'; optional for structured events where metadata carries the payload.

        timestamp : typing.Optional[dt.datetime]
            Timestamp for the transcript (optional, will be used as created_at)

        event_type : typing.Optional[EventTypeEnum]
            Discriminator for rich transcript events. Defaults to 'message' for backward compatibility.

            * `message` - Message
            * `step_enter` - Step Enter
            * `step_exit` - Step Exit
            * `step_transition` - Step Transition
            * `node_transition` - Node Transition
            * `variable_commit` - Variable Commit
            * `variable_rejected` - Variable Rejected
            * `run_tool_dispatched` - Run Tool Dispatched
            * `run_tool_completed` - Run Tool Completed

        metadata : typing.Optional[typing.Any]
            Structured payload for non-message events. Must be a JSON object.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSessionsTranscriptsCreateResponse


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.sessions_transcripts_create(
            id=1,
            text_src="User",
        )
        """
        _response = self._raw_client.sessions_transcripts_create(
            id,
            text_src=text_src,
            text=text,
            timestamp=timestamp,
            event_type=event_type,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def sessions_variables_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceSessionsVariablesRetrieveResponse:
        """
        Return extracted conversation variables for this session, excluding private variables

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSessionsVariablesRetrieveResponse


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.sessions_variables_retrieve(
            id=1,
        )
        """
        _response = self._raw_client.sessions_variables_retrieve(id, request_options=request_options)
        return _response.data

    def sessions_terminal_by_workflow_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        ``POST /api/v1/voice/sessions/terminal-by-workflow/``

        Marks a ``VoiceInteractionSession`` terminal when no bot lifecycle
        will write it (AWS Connect no-answer/busy/failed paths). Looks up
        the session via the ZEN-204 ``OutboundCallCorrelation.session_id``
        bridge, atomically updates the parent + child tables under a
        WHERE-IN guard so already-active rows are no-ops.

        Tenant scoping: ``integration_workflow_id`` is globally unique, so
        the row's tenant is implicit (same pattern as the abandon endpoint).
        Service-token auth bypasses the RLS middleware; we set
        ``app.current_tenant_id`` GUC explicitly inside the transaction.

        Request body::

            {
                "integration_workflow_id": "<temporal-wf-id>",
                "call_outcome": "no_answer" | "busy" | "failed" | "canceled" | "answered" | ...,
                "duration_seconds": <int>,           # optional, default 0
                "raw_disconnect_reason": "<str>"     # optional, default ""
            }

        Responses (always 200 — best-effort writer; failures are diagnostic):
            ``{"updated": true,  "session_id": <int>, "reason": "updated"}``
            ``{"updated": false, "reason": "correlation_not_found"}``
            ``{"updated": false, "reason": "no_session_bound"}``  # pre-ZEN-204 voice-py
            ``{"updated": false, "session_id": <int>, "reason": "already_terminal"}``
            400 — validation error
            401 — invalid/missing service token

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.sessions_terminal_by_workflow_create()
        """
        _response = self._raw_client.sessions_terminal_by_workflow_create(request_options=request_options)
        return _response.data

    def transcribers_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        workflow: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedTranscriberList:
        """
        List all Deepgram Transcribers matching the applied filters

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        workflow : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedTranscriberList


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.transcribers_list()
        """
        _response = self._raw_client.transcribers_list(
            page=page, page_size=page_size, workflow=workflow, request_options=request_options
        )
        return _response.data

    def transcribers_create(
        self, *, request: Transcriber, request_options: typing.Optional[RequestOptions] = None
    ) -> Transcriber:
        """
        Create a new Deepgram Transcriber.

        Parameters
        ----------
        request : Transcriber

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Transcriber


        Examples
        --------
        from zenlabs_sdk import Transcriber_DeepgramTranscriber, ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.transcribers_create(
            request=Transcriber_DeepgramTranscriber(
                workflow=1,
            ),
        )
        """
        _response = self._raw_client.transcribers_create(request=request, request_options=request_options)
        return _response.data

    def transcribers_retrieve(self, id: int, *, request_options: typing.Optional[RequestOptions] = None) -> Transcriber:
        """
        Retrieve the specified Deepgram Transcriber.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Transcriber.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Transcriber


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.transcribers_retrieve(
            id=1,
        )
        """
        _response = self._raw_client.transcribers_retrieve(id, request_options=request_options)
        return _response.data

    def transcribers_update(
        self, id: int, *, request: Transcriber, request_options: typing.Optional[RequestOptions] = None
    ) -> Transcriber:
        """
        Update the specified Deepgram Transcriber.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Transcriber.

        request : Transcriber

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Transcriber


        Examples
        --------
        from zenlabs_sdk import Transcriber_DeepgramTranscriber, ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.transcribers_update(
            id=1,
            request=Transcriber_DeepgramTranscriber(
                workflow=1,
            ),
        )
        """
        _response = self._raw_client.transcribers_update(id, request=request, request_options=request_options)
        return _response.data

    def transcribers_destroy(self, id: int, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete the specified Deepgram Transcriber. This may archive/delete related objects.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Transcriber.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.transcribers_destroy(
            id=1,
        )
        """
        _response = self._raw_client.transcribers_destroy(id, request_options=request_options)
        return _response.data

    def transcribers_partial_update(
        self, id: int, *, request: PatchedTranscriber, request_options: typing.Optional[RequestOptions] = None
    ) -> Transcriber:
        """
        Update the specified Deepgram Transcriber.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Transcriber.

        request : PatchedTranscriber

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Transcriber


        Examples
        --------
        from zenlabs_sdk import PatchedTranscriber_DeepgramTranscriber, ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.transcribers_partial_update(
            id=1,
            request=PatchedTranscriber_DeepgramTranscriber(),
        )
        """
        _response = self._raw_client.transcribers_partial_update(id, request=request, request_options=request_options)
        return _response.data

    def transcribers_switch_resourcetype_create(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Replace a transcriber row with a different child type.

        The ``workflow`` OneToOneField unique constraint blocks an
        insert-then-delete approach, so we hard-delete the old row inside
        an atomic block and recreate via the polymorphic serializer.
        ``resourcetype`` in the payload picks the new child type; the rest
        of the body is forwarded to the serializer for validation.

        Accepted ``resourcetype`` values come from
        ``TranscriberPolymorphicSerializer.valid_resourcetypes()``, derived
        from ``model_serializer_mapping`` — adding a new child type only
        requires a mapping entry.

        Field-carryover semantics: any field omitted from the payload would
        otherwise reset to the model default on the freshly inserted row.
        For workspace-secret links (``api_key_secret``) and the human
        ``title`` we explicitly carry the value over from the old row when
        the payload omits the key — so callers (UI or API) don't silently
        drop the secret link or the user-given name on a type switch.
        Explicit ``"api_key_secret": null`` / ``"title": null`` in the body
        still clear them (we use key-presence, not truthiness, so explicit-
        null is honored).

        Errors during serializer validation propagate as
        ``ValidationError`` (400) and Django auto-rolls back the txn.

        .. note::
            ``QuerySet.delete()`` fires ``pre_delete``/``post_delete`` signals on
            the old row. Receivers that perform external side effects (webhooks,
            cleanup tasks) will trigger spuriously on every type switch. If you
            add such a receiver, gate it on ``not getattr(instance, "_swapping",
            False)`` or similar.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Transcriber.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.transcribers_switch_resourcetype_create(
            id=1,
        )
        """
        _response = self._raw_client.transcribers_switch_resourcetype_create(id, request_options=request_options)
        return _response.data

    def voice_providers_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        workflow: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedVoiceProviderList:
        """
        List all Cartesia Voice Providers matching the applied filters

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        workflow : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedVoiceProviderList


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.voice_providers_list()
        """
        _response = self._raw_client.voice_providers_list(
            page=page, page_size=page_size, workflow=workflow, request_options=request_options
        )
        return _response.data

    def voice_providers_create(
        self, *, request: VoiceProvider, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceProvider:
        """
        Create a new Cartesia Voice Provider.

        Parameters
        ----------
        request : VoiceProvider

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceProvider


        Examples
        --------
        from zenlabs_sdk import VoiceProvider_CartesiaVoiceProvider, ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.voice_providers_create(
            request=VoiceProvider_CartesiaVoiceProvider(
                workflow=1,
            ),
        )
        """
        _response = self._raw_client.voice_providers_create(request=request, request_options=request_options)
        return _response.data

    def voice_providers_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceProvider:
        """
        Retrieve the specified Cartesia Voice Provider.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Voice Provider.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceProvider


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.voice_providers_retrieve(
            id=1,
        )
        """
        _response = self._raw_client.voice_providers_retrieve(id, request_options=request_options)
        return _response.data

    def voice_providers_update(
        self, id: int, *, request: VoiceProvider, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceProvider:
        """
        Update the specified Cartesia Voice Provider.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Voice Provider.

        request : VoiceProvider

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceProvider


        Examples
        --------
        from zenlabs_sdk import VoiceProvider_CartesiaVoiceProvider, ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.voice_providers_update(
            id=1,
            request=VoiceProvider_CartesiaVoiceProvider(
                workflow=1,
            ),
        )
        """
        _response = self._raw_client.voice_providers_update(id, request=request, request_options=request_options)
        return _response.data

    def voice_providers_destroy(self, id: int, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete the specified Cartesia Voice Provider. This may archive/delete related objects.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Voice Provider.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.voice_providers_destroy(
            id=1,
        )
        """
        _response = self._raw_client.voice_providers_destroy(id, request_options=request_options)
        return _response.data

    def voice_providers_partial_update(
        self, id: int, *, request: PatchedVoiceProvider, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceProvider:
        """
        Update the specified Cartesia Voice Provider.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Voice Provider.

        request : PatchedVoiceProvider

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceProvider


        Examples
        --------
        from zenlabs_sdk import PatchedVoiceProvider_CartesiaVoiceProvider, ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.voice_providers_partial_update(
            id=1,
            request=PatchedVoiceProvider_CartesiaVoiceProvider(),
        )
        """
        _response = self._raw_client.voice_providers_partial_update(
            id, request=request, request_options=request_options
        )
        return _response.data

    def voice_providers_switch_resourcetype_create(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Replace a voice provider row with a different child type.

        The ``workflow`` OneToOneField unique constraint blocks an
        insert-then-delete approach, so we hard-delete the old row inside
        an atomic block and recreate via the polymorphic serializer.
        ``resourcetype`` in the payload picks the new child type; the rest
        of the body is forwarded to the serializer for validation.

        Accepted ``resourcetype`` values come from
        ``VoiceProviderPolymorphicSerializer.valid_resourcetypes()``, derived
        from ``model_serializer_mapping`` — adding a new child type only
        requires a mapping entry.

        Field-carryover semantics: any field omitted from the payload would
        otherwise reset to the model default on the freshly inserted row.
        For workspace-secret links (``api_key_secret``) and the human
        ``title`` we explicitly carry the value over from the old row when
        the payload omits the key — so callers (UI or API) don't silently
        drop the secret link or the user-given name on a type switch.
        Explicit ``"api_key_secret": null`` / ``"title": null`` in the body
        still clear them (we use key-presence, not truthiness, so explicit-
        null is honored).

        Errors during serializer validation propagate as
        ``ValidationError`` (400) and Django auto-rolls back the txn.

        .. note::
            ``QuerySet.delete()`` fires ``pre_delete``/``post_delete`` signals on
            the old row. Receivers that perform external side effects (webhooks,
            cleanup tasks) will trigger spuriously on every type switch. If you
            add such a receiver, gate it on ``not getattr(instance, "_swapping",
            False)`` or similar.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Voice Provider.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.voice_providers_switch_resourcetype_create(
            id=1,
        )
        """
        _response = self._raw_client.voice_providers_switch_resourcetype_create(id, request_options=request_options)
        return _response.data

    def workflow_config_by_id_retrieve(
        self, workflow_id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> WorkflowConfiguration:
        """
        Fetch workflow, nodes, edges, and provider config for a specific workflow ID.

        Parameters
        ----------
        workflow_id : int
            The workflow ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WorkflowConfiguration


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.workflow_config_by_id_retrieve(
            workflow_id=1,
        )
        """
        _response = self._raw_client.workflow_config_by_id_retrieve(workflow_id, request_options=request_options)
        return _response.data

    def workflow_config_retrieve(
        self,
        phone_number: str,
        *,
        direction: VoiceWorkflowConfigRetrieveRequestDirection,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> WorkflowConfiguration:
        """
        Fetch workflow, nodes, edges, and provider config for a specific phone number.

        Parameters
        ----------
        phone_number : str
            The phone number (E.164 format, e.g., +15550001111)

        direction : VoiceWorkflowConfigRetrieveRequestDirection
            Call direction ('inbound' or 'outbound')

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WorkflowConfiguration


        Examples
        --------
        from zenlabs_sdk import ZenClient

        client = ZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )
        client.voice.workflow_config_retrieve(
            phone_number="phone_number",
            direction="inbound",
        )
        """
        _response = self._raw_client.workflow_config_retrieve(
            phone_number, direction=direction, request_options=request_options
        )
        return _response.data


class AsyncVoiceClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawVoiceClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawVoiceClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawVoiceClient
        """
        return self._raw_client

    async def amazon_connect_available_numbers_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        List phone numbers claimed on a specific Connect instance.

        Already-imported numbers are flagged by the upstream voice-py route; the
        browser renders those as disabled options without an extra round-trip.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.amazon_connect_available_numbers_retrieve()


        asyncio.run(main())
        """
        _response = await self._raw_client.amazon_connect_available_numbers_retrieve(request_options=request_options)
        return _response.data

    async def amazon_connect_contact_flows_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        List contact flows for a specific Connect instance.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.amazon_connect_contact_flows_retrieve()


        asyncio.run(main())
        """
        _response = await self._raw_client.amazon_connect_contact_flows_retrieve(request_options=request_options)
        return _response.data

    async def amazon_connect_instances_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        List Connect instances visible to a given Nango AWS IAM connection.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.amazon_connect_instances_retrieve()


        asyncio.run(main())
        """
        _response = await self._raw_client.amazon_connect_instances_retrieve(request_options=request_options)
        return _response.data

    async def api_keys_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedApiKeyListList:
        """
        Base class mixin for model view sets. Implemented as a mixin to allow
        flexibility in the inheritance chain.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedApiKeyListList


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.api_keys_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_keys_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def api_keys_create(
        self,
        *,
        name: str,
        id: typing.Optional[int] = OMIT,
        key: typing.Optional[str] = OMIT,
        prefix: typing.Optional[str] = OMIT,
        scopes: typing.Optional[typing.Sequence[str]] = OMIT,
        expires_at: typing.Optional[dt.datetime] = OMIT,
        created_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiKeyCreate:
        """
        Base class mixin for model view sets. Implemented as a mixin to allow
        flexibility in the inheritance chain.

        Parameters
        ----------
        name : str
            Human label (e.g. 'Production backend').

        id : typing.Optional[int]

        key : typing.Optional[str]

        prefix : typing.Optional[str]
            First 16 chars of the key (e.g. 'zn_live_a1b2c3d4'). For lookup and dashboard display.

        scopes : typing.Optional[typing.Sequence[str]]

        expires_at : typing.Optional[dt.datetime]
            Optional. NULL = never expires (until revoked).

        created_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiKeyCreate


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.api_keys_create(
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_keys_create(
            name=name,
            id=id,
            key=key,
            prefix=prefix,
            scopes=scopes,
            expires_at=expires_at,
            created_at=created_at,
            request_options=request_options,
        )
        return _response.data

    async def api_keys_destroy(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Base class mixin for model view sets. Implemented as a mixin to allow
        flexibility in the inheritance chain.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.api_keys_destroy(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_keys_destroy(id, request_options=request_options)
        return _response.data

    async def checkpoint_create(
        self,
        *,
        session_id: int,
        node_name: str,
        extracted_data: typing.Any,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> VoiceSession:
        """
        Save extracted variables to the session checkpoint store and return updated state

        Parameters
        ----------
        session_id : int

        node_name : str
            Node name used to look up the node within the session workflow

        extracted_data : typing.Any

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSession


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.checkpoint_create(
                session_id=1,
                node_name="node_name",
                extracted_data={"key": "value"},
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.checkpoint_create(
            session_id=session_id, node_name=node_name, extracted_data=extracted_data, request_options=request_options
        )
        return _response.data

    async def dashboard_sessions_list(
        self,
        *,
        direction: typing.Optional[VoiceDashboardSessionsListRequestDirection] = None,
        disposition: typing.Optional[VoiceDashboardSessionsListRequestDisposition] = None,
        disposition_sentiment: typing.Optional[VoiceDashboardSessionsListRequestDispositionSentiment] = None,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        status: typing.Optional[VoiceDashboardSessionsListRequestStatus] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedInteractionSessionList:
        """
        List all Voice Sessions matching the applied filters

        Parameters
        ----------
        direction : typing.Optional[VoiceDashboardSessionsListRequestDirection]
            * `inbound` - Inbound
            * `outbound` - Outbound

        disposition : typing.Optional[VoiceDashboardSessionsListRequestDisposition]
            Computed interaction disposition category (ZEN-71).

            * `agent_escalation_immediate` - Agent Escalation Immediate
            * `agent_escalation_mid_call` - Agent Escalation Mid-Call
            * `hangup_immediate` - Hangup Immediate
            * `hangup_mid_call` - Hangup Mid-Call
            * `transfer_unsupported` - Transfer Unsupported
            * `resolved` - Resolved
            * `post_resolution_transfer` - Post Resolution Transfer
            * `fatal_error_escalated` - Fatal Error Escalated
            * `error` - Error

        disposition_sentiment : typing.Optional[VoiceDashboardSessionsListRequestDispositionSentiment]
            Sentiment derived from the disposition (ZEN-71).

            * `positive` - Positive
            * `neutral` - Neutral
            * `negative` - Negative

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        search : typing.Optional[str]
            A search term.

        status : typing.Optional[VoiceDashboardSessionsListRequestStatus]
            * `created` - Created
            * `queued` - Queued
            * `active` - Active
            * `completed` - Completed
            * `failed` - Failed
            * `cancelled` - Cancelled

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedInteractionSessionList


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.dashboard_sessions_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.dashboard_sessions_list(
            direction=direction,
            disposition=disposition,
            disposition_sentiment=disposition_sentiment,
            page=page,
            page_size=page_size,
            search=search,
            status=status,
            request_options=request_options,
        )
        return _response.data

    async def dashboard_sessions_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> InteractionSession:
        """
        Retrieve the specified Voice Session.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Interaction Session.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        InteractionSession


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.dashboard_sessions_retrieve(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.dashboard_sessions_retrieve(id, request_options=request_options)
        return _response.data

    async def dashboard_sessions_transcripts_list(
        self,
        id: int,
        *,
        direction: typing.Optional[VoiceDashboardSessionsTranscriptsListRequestDirection] = None,
        disposition: typing.Optional[VoiceDashboardSessionsTranscriptsListRequestDisposition] = None,
        disposition_sentiment: typing.Optional[VoiceDashboardSessionsTranscriptsListRequestDispositionSentiment] = None,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        status: typing.Optional[VoiceDashboardSessionsTranscriptsListRequestStatus] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedTranscriptPostList:
        """
        Returns transcript posts for a session's conversation. Admin users (is_staff) see all rows including rich step/variable events; non-admin users see only legacy message rows.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Interaction Session.

        direction : typing.Optional[VoiceDashboardSessionsTranscriptsListRequestDirection]
            * `inbound` - Inbound
            * `outbound` - Outbound

        disposition : typing.Optional[VoiceDashboardSessionsTranscriptsListRequestDisposition]
            Computed interaction disposition category (ZEN-71).

            * `agent_escalation_immediate` - Agent Escalation Immediate
            * `agent_escalation_mid_call` - Agent Escalation Mid-Call
            * `hangup_immediate` - Hangup Immediate
            * `hangup_mid_call` - Hangup Mid-Call
            * `transfer_unsupported` - Transfer Unsupported
            * `resolved` - Resolved
            * `post_resolution_transfer` - Post Resolution Transfer
            * `fatal_error_escalated` - Fatal Error Escalated
            * `error` - Error

        disposition_sentiment : typing.Optional[VoiceDashboardSessionsTranscriptsListRequestDispositionSentiment]
            Sentiment derived from the disposition (ZEN-71).

            * `positive` - Positive
            * `neutral` - Neutral
            * `negative` - Negative

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        search : typing.Optional[str]
            A search term.

        status : typing.Optional[VoiceDashboardSessionsTranscriptsListRequestStatus]
            * `created` - Created
            * `queued` - Queued
            * `active` - Active
            * `completed` - Completed
            * `failed` - Failed
            * `cancelled` - Cancelled

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedTranscriptPostList


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.dashboard_sessions_transcripts_list(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.dashboard_sessions_transcripts_list(
            id,
            direction=direction,
            disposition=disposition,
            disposition_sentiment=disposition_sentiment,
            page=page,
            page_size=page_size,
            search=search,
            status=status,
            request_options=request_options,
        )
        return _response.data

    async def integration_connections_aws_auth_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Resolve AWS IAM credentials by ``IntegrationConnection`` pk.

        Service-token-only. Used by voice-py's import-drawer read endpoints
        (Phase 12.5) which list Connect instances, contact flows, and phone
        numbers *before* any ``AwsConnectPhoneNumber`` row exists.

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.integration_connections_aws_auth_retrieve(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.integration_connections_aws_auth_retrieve(
            id, request_options=request_options
        )
        return _response.data

    async def internal_sessions_retry_summary_create(
        self, session_id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        ``POST /api/v1/voice/internal/sessions/<int:session_id>/retry-summary/``

        Service-token authenticated. Returns 200 with ``persisted: false`` in
        the ack-only phase; the field flips to ``true`` once the in-place
        regeneration lands.

        Parameters
        ----------
        session_id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.internal_sessions_retry_summary_create(
                session_id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.internal_sessions_retry_summary_create(
            session_id, request_options=request_options
        )
        return _response.data

    async def langsmith_analytics_list(
        self,
        *,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[TraceListItem]:
        """
        List recent conversation traces with summary metrics.

        Parameters
        ----------
        limit : typing.Optional[int]
            Number of traces to return (default 20, max 100)

        offset : typing.Optional[int]
            Offset for pagination (default 0)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[TraceListItem]


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.langsmith_analytics_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.langsmith_analytics_list(
            limit=limit, offset=offset, request_options=request_options
        )
        return _response.data

    async def langsmith_analytics_analytics_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> TraceAnalytics:
        """
        Fetch detailed analytics for a specific LangSmith trace, including cost, latency, token usage, and per-turn breakdown.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        TraceAnalytics


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.langsmith_analytics_analytics_retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.langsmith_analytics_analytics_retrieve(id, request_options=request_options)
        return _response.data

    async def list_voices_retrieve(
        self, *, workflow_id: int, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Return available voices from the workflow's configured TTS provider. Results are cached for 5 minutes per provider instance.

        Parameters
        ----------
        workflow_id : int
            Workflow ID whose voice provider to query.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.list_voices_retrieve(
                workflow_id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_voices_retrieve(
            workflow_id=workflow_id, request_options=request_options
        )
        return _response.data

    async def outbound_correlations_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        ``POST /api/v1/voice/outbound-correlations/``

        Records a new correlation row at AWS dispatch time. Idempotent on
        both ``aws_contact_id`` (AWS retry double-billing) and
        ``integration_workflow_id`` (Temporal retry that produced different
        AWS ContactIds for the same logical dial).

        Request body::

            {
                "aws_contact_id": "<connect-contact-id>",
                "source_phone": "<E.164>",
                "destination_phone": "<E.164>",
                "integration_workflow_id": "<temporal-wf-id>",
                "phone_number_id": <int>,
                "tenant_id": <int>
            }

        Responses:
            201 ``{"id": <int>}`` — new row inserted
            200 ``{"id": <int>, "duplicate": "<aws_contact_id|integration_workflow_id>"}`` — idempotent hit
            400 — validation error
            401 — invalid/missing service token

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.outbound_correlations_create()


        asyncio.run(main())
        """
        _response = await self._raw_client.outbound_correlations_create(request_options=request_options)
        return _response.data

    async def outbound_correlations_abandon_create(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        ``POST /api/v1/voice/outbound-correlations/abandon/``

        Marks a correlation row as abandoned so future claims for the same
        ``(source_phone, destination_phone)`` skip it. Called by
        integrations-py via voice-py at ``OutboundCallWorkflow`` exit when
        no Twilio CallSid was bound to the workflow.

        Race precedence: claim wins. The atomic UPDATE uses
        ``WHERE claimed_at IS NULL AND abandoned_at IS NULL`` so an
        in-flight claim that committed between the workflow's exit
        decision and this request lands first; the abandon then no-ops
        and we return ``{"abandoned": false, "reason": "already_claimed"}``.

        Best-effort: failures here don't block the workflow. The retention
        sweeper (3-day TTL) is the durable safety net — abandon is purely
        opportunistic cleanup that releases the (source, destination) slot
        sooner than ``claim_expires_at`` (2 min) would.

        Tenant scoping: integration_workflow_id is globally unique, so we
        don't require a tenant_id parameter — the row's tenant is implicit
        in the workflow id. (Same pattern as record_outbound_correlation's
        tenant inference from phone_number_id.)

        Request body::

            {
                "integration_workflow_id": "<temporal-wf-id>",
                "reason": "<short_tag>",  # optional, defaults to ""
            }

        Responses:
            200 ``{"abandoned": true,  "id": <int>}`` — row marked
            200 ``{"abandoned": false, "reason": "already_claimed"}``
            200 ``{"abandoned": false, "reason": "not_found"}``
            200 ``{"abandoned": false, "reason": "already_abandoned"}``
            400 — validation error
            401 — invalid/missing service token

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.outbound_correlations_abandon_create()


        asyncio.run(main())
        """
        _response = await self._raw_client.outbound_correlations_abandon_create(request_options=request_options)
        return _response.data

    async def outbound_correlations_by_call_sid_retrieve(
        self, call_sid: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        ``GET /api/v1/voice/outbound-correlations/by-call-sid/<call_sid>/?tenant_id=<int>``

        Look up an already-claimed correlation row by its Twilio CallSid so
        the voice-py status-callback handler can forward
        ``on_call_status`` to the integrations-py ``OutboundCallWorkflow``.
        Without this signal the workflow blocks on ``wait_condition`` until
        its ``run_timeout_seconds`` elapses and resolves as ``timeout``,
        even when ``on_call_enrichment`` has already arrived.

        Service-token auth + **mandatory tenant scoping** via the
        ``tenant_id`` query param. CallSids are not secret (they appear in
        Twilio console, status callbacks, and logs), so a global lookup
        would let any service-token holder probe known CallSids and
        enumerate correlations across tenants. The caller (voice-py) has
        ``tenant_id`` from the cached session row, so the requirement is
        free at the call site.

        Responses:
            200 ``{"integration_workflow_id": <str>, "tenant_id": <int>}``
            404 — no row claimed under that call_sid for the given tenant
            400 — missing/non-integer tenant_id query param
            401 — invalid/missing service token

        Parameters
        ----------
        call_sid : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.outbound_correlations_by_call_sid_retrieve(
                call_sid="call_sid",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.outbound_correlations_by_call_sid_retrieve(
            call_sid, request_options=request_options
        )
        return _response.data

    async def outbound_correlations_claim_create(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        ``POST /api/v1/voice/outbound-correlations/claim/``

        Atomically claims the oldest unclaimed correlation matching
        ``(source_phone, destination_phone)`` whose ``claim_expires_at`` is
        still in the future (Phase 14: column-driven 2-min TTL replaces
        the legacy 180s ``created_at``-derived window).

        Reconnect path: if a row with this ``twilio_call_sid`` already
        exists (Twilio webhook retry on the same call), return its
        ``integration_workflow_id`` directly without re-claiming. The
        reconnect lookup is intentionally NOT gated on
        ``claim_expires_at`` / ``abandoned_at`` — once a row is claimed
        its workflow id is the durable answer for that CallSid until
        retention expires, regardless of TTL/abandon state.

        Single-statement claim: ``UPDATE ... WHERE id=(SELECT ... FOR
        UPDATE SKIP LOCKED LIMIT 1) RETURNING ...``. Survives Supabase
        transaction-mode pooling because it's one round-trip; no FOR UPDATE
        held across statements.

        Request body::

            {
                "source_phone": "<Twilio.To, E.164>",
                "destination_phone": "<Twilio.From, E.164>",
                "twilio_call_sid": "<CA...>",
                "tenant_id": <int>
            }

        Responses:
            200 ``{"integration_workflow_id": "<id>"}`` — hit
            204 — no eligible row in the claim window
            401 — invalid/missing service token

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.outbound_correlations_claim_create()


        asyncio.run(main())
        """
        _response = await self._raw_client.outbound_correlations_claim_create(request_options=request_options)
        return _response.data

    async def outbound_correlations_sweep_create(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        ``POST /api/v1/voice/outbound-correlations/sweep/``

        Phase 14: deletes correlation rows past their
        ``retention_expires_at`` deadline. Service-token only — designed
        to be invoked by a Temporal scheduled workflow (see
        ``ScheduledTriggerWorkflow``) on a daily cadence at low traffic.

        Cross-tenant by design (the sweeper has no tenant context and
        retention is uniform across tenants); ``disable_auto_filters``
        bypasses the tenant ContextVar manager filter so all expired
        rows surface for the same DELETE.

        The implementation mirrors ``reap_outbound_correlations`` so an
        operator can either run the management command on a host (e.g.
        one-shot ad-hoc cleanup) OR rely on the Temporal-driven HTTP
        path for production. Both paths converge on the same
        ``retention_expires_at < now()`` predicate.

        Hard upper bound: ``MAX_DELETES_PER_REQUEST`` caps the total number
        of rows a single sweep call may delete (defense-in-depth against a
        misconfigured schedule firing every minute or a token-compromise
        blast). Operators can re-invoke the endpoint or run the
        ``reap_outbound_correlations`` management command for unbounded
        one-shot cleanups.

        Request body: ``{"batch_size": <int, default=1000>}`` (optional)
        Response: 200 ``{"deleted": <int>, "cutoff": "<ISO>", "capped": <bool>}``

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.outbound_correlations_sweep_create()


        asyncio.run(main())
        """
        _response = await self._raw_client.outbound_correlations_sweep_create(request_options=request_options)
        return _response.data

    async def phone_numbers_list(
        self,
        *,
        inbound_workflow: typing.Optional[int] = None,
        outbound_workflow: typing.Optional[int] = None,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPhoneNumberList:
        """
        List all Twilio Phone Numbers matching the applied filters

        Parameters
        ----------
        inbound_workflow : typing.Optional[int]

        outbound_workflow : typing.Optional[int]

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        search : typing.Optional[str]
            A search term.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPhoneNumberList


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.phone_numbers_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.phone_numbers_list(
            inbound_workflow=inbound_workflow,
            outbound_workflow=outbound_workflow,
            page=page,
            page_size=page_size,
            search=search,
            request_options=request_options,
        )
        return _response.data

    async def phone_numbers_create(
        self, *, request: PhoneNumber, request_options: typing.Optional[RequestOptions] = None
    ) -> PhoneNumber:
        """
        Create a new Twilio Phone Number.

        Parameters
        ----------
        request : PhoneNumber

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PhoneNumber


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient, PhoneNumber_TwilioPhoneNumber

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.phone_numbers_create(
                request=PhoneNumber_TwilioPhoneNumber(
                    phone_number="phone_number",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.phone_numbers_create(request=request, request_options=request_options)
        return _response.data

    async def phone_numbers_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PhoneNumber:
        """
        Retrieve the specified Twilio Phone Number.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Phone Number.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PhoneNumber


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.phone_numbers_retrieve(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.phone_numbers_retrieve(id, request_options=request_options)
        return _response.data

    async def phone_numbers_update(
        self, id: int, *, request: PhoneNumber, request_options: typing.Optional[RequestOptions] = None
    ) -> PhoneNumber:
        """
        Update the specified Twilio Phone Number.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Phone Number.

        request : PhoneNumber

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PhoneNumber


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient, PhoneNumber_TwilioPhoneNumber

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.phone_numbers_update(
                id=1,
                request=PhoneNumber_TwilioPhoneNumber(
                    phone_number="phone_number",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.phone_numbers_update(id, request=request, request_options=request_options)
        return _response.data

    async def phone_numbers_destroy(self, id: int, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete the specified Twilio Phone Number. This may archive/delete related objects.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Phone Number.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.phone_numbers_destroy(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.phone_numbers_destroy(id, request_options=request_options)
        return _response.data

    async def phone_numbers_partial_update(
        self, id: int, *, request: PatchedPhoneNumber, request_options: typing.Optional[RequestOptions] = None
    ) -> PhoneNumber:
        """
        Update the specified Twilio Phone Number.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Phone Number.

        request : PatchedPhoneNumber

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PhoneNumber


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient, PatchedPhoneNumber_TwilioPhoneNumber

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.phone_numbers_partial_update(
                id=1,
                request=PatchedPhoneNumber_TwilioPhoneNumber(),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.phone_numbers_partial_update(
            id, request=request, request_options=request_options
        )
        return _response.data

    async def phone_numbers_auth_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Return Twilio credentials for a phone number (service-token only).

        Used by the voice service to fetch Twilio credentials at activity
        runtime rather than storing them in Temporal workflow history.

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.phone_numbers_auth_retrieve(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.phone_numbers_auth_retrieve(id, request_options=request_options)
        return _response.data

    async def phone_numbers_config_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Return non-credential phone number config (iPaaS service-token only).

        Used by the iPaaS pre-call pipeline to derive ``from_phone`` and the
        default outbound workflow id from a ``phone_number_id``, so schedules
        only need ``phone_number_id`` instead of duplicating these fields.

        Distinct from ``PhoneNumberAuthView`` (which exposes Twilio credentials
        to voice-py via ``ZENAPI_VOICE_SERVICE_TOKEN``) — this endpoint:
        - NEVER returns auth_token/account_sid
        - Authenticates via ``ZENAPI_INTEGRATIONS_SERVICE_TOKEN`` (iPaaS identity)

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.phone_numbers_config_retrieve(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.phone_numbers_config_retrieve(id, request_options=request_options)
        return _response.data

    async def phone_numbers_resync_webhooks_create(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Re-attempt Twilio webhook configuration for an existing phone.

        Surfaces the failure reason via a non-2xx so the UI can flash the
        exact Twilio/Nango message (instead of silently retrying).

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Phone Number.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.phone_numbers_resync_webhooks_create(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.phone_numbers_resync_webhooks_create(id, request_options=request_options)
        return _response.data

    async def phone_numbers_by_phone_retrieve(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        ``GET /api/v1/voice/phone-numbers/by-phone/?phone=<E.164>``

        Lightweight service-token lookup that resolves an E.164 phone string
        to the tenant + ``phone_number_id`` of an ``AwsConnectPhoneNumber``
        row that owns it. Matches on either ``bridge_phone`` (the Twilio
        BYOC bridge number AWS Connect dials) OR ``phone_number`` (the AWS
        caller-ID for single-number deploys).

        Used by voice-py's Twilio BYOC inbound webhook (ZEN-205) to get
        tenant context BEFORE creating a session, so it can claim the
        outbound correlation and ``attach_call_sid`` to the pre-existing
        dispatch session — the bridge that makes per-call ``variables``
        reach the bot for AWS-Connect-originated calls.

        Non-AWS phones return 404. Voice-py treats 404 as "this isn't a
        BYOC bridge inbound, fall through to legacy session-creation
        path." That preserves regular Twilio inbound behavior.

        Phone numbers are globally unique (``BasePhoneNumber`` has a
        unique constraint on ``phone_number`` for ``is_deleted=False``),
        so at most one row matches per query — no cross-tenant
        disambiguation needed.

        Responses:
            200 ``{"tenant_id": <int>, "phone_number_id": <int>, "provider": "amazon_connect"}``
            404 — no AwsConnectPhoneNumber owns this number
            400 — missing/empty ``phone`` query param
            401 — invalid/missing service token

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.phone_numbers_by_phone_retrieve()


        asyncio.run(main())
        """
        _response = await self._raw_client.phone_numbers_by_phone_retrieve(request_options=request_options)
        return _response.data

    async def preview_voice_create(
        self,
        *,
        workflow_id: int,
        text: typing.Optional[str] = OMIT,
        voice_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Stream a short TTS audio sample using the workflow's configured voice provider. The API key is resolved from the workspace secret or the provider's encrypted key — never sent from the client.

        Parameters
        ----------
        workflow_id : int
            Workflow ID whose voice provider to preview.

        text : typing.Optional[str]
            Text to synthesize. Defaults to a short greeting.

        voice_id : typing.Optional[str]
            Override voice ID (e.g. when previewing a preset before saving).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.preview_voice_create(
                workflow_id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.preview_voice_create(
            workflow_id=workflow_id, text=text, voice_id=voice_id, request_options=request_options
        )
        return _response.data

    async def provider_metadata_retrieve(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Return the catalog of voice provider + transcriber enum values.

        Powers UI dropdowns for model selection, emotion picking, language
        selection, etc. without forcing the frontend to mirror Python enums.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.provider_metadata_retrieve()


        asyncio.run(main())
        """
        _response = await self._raw_client.provider_metadata_retrieve(request_options=request_options)
        return _response.data

    async def secrets_resolve_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Service-token endpoint that returns the plaintext secret value.

        Consumed by voice-py at call setup. Knox-token auth is rejected — this
        path bypasses the user-facing tenant-scoped manager and looks up the
        secret across tenants by id, so we tightly gate it on ``ServiceToken``.

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.secrets_resolve_retrieve(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.secrets_resolve_retrieve(id, request_options=request_options)
        return _response.data

    async def session_token_create(
        self, *, workflow_id: int, request_options: typing.Optional[RequestOptions] = None
    ) -> SessionTokenResponse:
        """
        Parameters
        ----------
        workflow_id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SessionTokenResponse


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.session_token_create(
                workflow_id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.session_token_create(
            workflow_id=workflow_id, request_options=request_options
        )
        return _response.data

    async def sessions_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedVoiceSessionList:
        """
        List all Voice Sessions matching the applied filters

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedVoiceSessionList


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.sessions_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.sessions_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def sessions_create(
        self,
        *,
        from_number: str,
        to_number: str,
        direction: DirectionEnum,
        provider_ref_id: typing.Optional[str] = OMIT,
        workflow_id: typing.Optional[int] = OMIT,
        variables: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> VoiceSession:
        """
        Create a new Voice Session.

        Parameters
        ----------
        from_number : str
            E.164 formatted from phone number

        to_number : str
            E.164 formatted to phone number

        direction : DirectionEnum
            Call direction (inbound or outbound)

            * `inbound` - Inbound
            * `outbound` - Outbound

        provider_ref_id : typing.Optional[str]
            The Call SID or external ID from the provider

        workflow_id : typing.Optional[int]
            Workflow ID for browser sessions (bypasses phone number lookup)

        variables : typing.Optional[typing.Any]
            Campaign contact variables passed to the agent

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSession


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.sessions_create(
                from_number="from_number",
                to_number="to_number",
                direction="inbound",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.sessions_create(
            from_number=from_number,
            to_number=to_number,
            direction=direction,
            provider_ref_id=provider_ref_id,
            workflow_id=workflow_id,
            variables=variables,
            request_options=request_options,
        )
        return _response.data

    async def sessions_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceSession:
        """
        Retrieve the specified Voice Session.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSession


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.sessions_retrieve(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.sessions_retrieve(id, request_options=request_options)
        return _response.data

    async def sessions_update(
        self,
        id_: int,
        *,
        from_number: str,
        to_number: str,
        id: typing.Optional[int] = OMIT,
        tenant_id: typing.Optional[int] = OMIT,
        conversation_id: typing.Optional[int] = OMIT,
        active_agent_id: typing.Optional[int] = OMIT,
        status: typing.Optional[Status9B1Enum] = OMIT,
        direction: typing.Optional[DirectionEnum] = OMIT,
        call_outcome: typing.Optional[CallOutcomeEnum] = OMIT,
        provider_ref_id: typing.Optional[str] = OMIT,
        duration_seconds: typing.Optional[int] = OMIT,
        recording_url: typing.Optional[str] = OMIT,
        provider_status: typing.Optional[str] = OMIT,
        variables: typing.Optional[typing.Any] = OMIT,
        summary: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        collected_variables: typing.Optional[typing.Any] = OMIT,
        summary_pending: typing.Optional[bool] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        ended_at: typing.Optional[dt.datetime] = OMIT,
        end_reason: typing.Optional[str] = OMIT,
        disposition: typing.Optional[DispositionEnum] = OMIT,
        disposition_sentiment: typing.Optional[DispositionSentimentEnum] = OMIT,
        user_turn_count: typing.Optional[int] = OMIT,
        created_at: typing.Optional[dt.datetime] = OMIT,
        updated_at: typing.Optional[dt.datetime] = OMIT,
        created_by: typing.Optional[User] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> VoiceSession:
        """
        Update the specified Voice Session.

        Parameters
        ----------
        id_ : int
            A unique integer value identifying this Voice Session.

        from_number : str
            E.164 formatted number

        to_number : str
            E.164 formatted number

        id : typing.Optional[int]

        tenant_id : typing.Optional[int]

        conversation_id : typing.Optional[int]
            The transcript associated with this interaction.

        active_agent_id : typing.Optional[int]
            The specific workflow handling this interaction.

        status : typing.Optional[Status9B1Enum]

        direction : typing.Optional[DirectionEnum]

        call_outcome : typing.Optional[CallOutcomeEnum]
            The telephony disposition.

            * `unknown` - Unknown
            * `answered` - Answered
            * `busy` - Busy
            * `no_answer` - No Answer
            * `failed` - Failed
            * `canceled` - Canceled
            * `voicemail` - Voicemail

        provider_ref_id : typing.Optional[str]
            The Call SID or external ID from the provider.

        duration_seconds : typing.Optional[int]
            Duration of the call in seconds

        recording_url : typing.Optional[str]
            Public or signed URL to the call audio.

        provider_status : typing.Optional[str]
            Raw status returned by the telephony provider.

        variables : typing.Optional[typing.Any]
            Campaign contact variables passed to the agent.

        summary : typing.Optional[str]
            LLM-generated one-sentence call summary.

        description : typing.Optional[str]
            LLM-generated 2-3 sentence call description.

        collected_variables : typing.Optional[typing.Any]
            LLM-extracted variables collected during the call (distinct from inbound campaign `variables`).

        summary_pending : typing.Optional[bool]
            True while the post-call summary pipeline owes this session a summary. Cleared on success or skip; the reconciler retries any row stuck True past the grace window.

        started_at : typing.Optional[dt.datetime]

        ended_at : typing.Optional[dt.datetime]

        end_reason : typing.Optional[str]

        disposition : typing.Optional[DispositionEnum]
            Computed interaction disposition category (ZEN-71).

            * `agent_escalation_immediate` - Agent Escalation Immediate
            * `agent_escalation_mid_call` - Agent Escalation Mid-Call
            * `hangup_immediate` - Hangup Immediate
            * `hangup_mid_call` - Hangup Mid-Call
            * `transfer_unsupported` - Transfer Unsupported
            * `resolved` - Resolved
            * `post_resolution_transfer` - Post Resolution Transfer
            * `fatal_error_escalated` - Fatal Error Escalated
            * `error` - Error

        disposition_sentiment : typing.Optional[DispositionSentimentEnum]
            Sentiment derived from the disposition (ZEN-71).

            * `positive` - Positive
            * `neutral` - Neutral
            * `negative` - Negative

        user_turn_count : typing.Optional[int]
            Number of user turns observed during the interaction. Reported by the runtime alongside disposition for analytics breakdowns (ZEN-71).

        created_at : typing.Optional[dt.datetime]

        updated_at : typing.Optional[dt.datetime]

        created_by : typing.Optional[User]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSession


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.sessions_update(
                id_=1,
                from_number="from_number",
                to_number="to_number",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.sessions_update(
            id_,
            from_number=from_number,
            to_number=to_number,
            id=id,
            tenant_id=tenant_id,
            conversation_id=conversation_id,
            active_agent_id=active_agent_id,
            status=status,
            direction=direction,
            call_outcome=call_outcome,
            provider_ref_id=provider_ref_id,
            duration_seconds=duration_seconds,
            recording_url=recording_url,
            provider_status=provider_status,
            variables=variables,
            summary=summary,
            description=description,
            collected_variables=collected_variables,
            summary_pending=summary_pending,
            started_at=started_at,
            ended_at=ended_at,
            end_reason=end_reason,
            disposition=disposition,
            disposition_sentiment=disposition_sentiment,
            user_turn_count=user_turn_count,
            created_at=created_at,
            updated_at=updated_at,
            created_by=created_by,
            request_options=request_options,
        )
        return _response.data

    async def sessions_destroy(self, id: int, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete the specified Voice Session. This may archive/delete related objects.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.sessions_destroy(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.sessions_destroy(id, request_options=request_options)
        return _response.data

    async def sessions_partial_update(
        self,
        id: int,
        *,
        status: typing.Optional[Status9B1Enum] = OMIT,
        direction: typing.Optional[DirectionEnum] = OMIT,
        call_outcome: typing.Optional[CallOutcomeEnum] = OMIT,
        provider_ref_id: typing.Optional[str] = OMIT,
        from_number: typing.Optional[str] = OMIT,
        to_number: typing.Optional[str] = OMIT,
        duration_seconds: typing.Optional[int] = OMIT,
        recording_url: typing.Optional[str] = OMIT,
        provider_status: typing.Optional[str] = OMIT,
        variables: typing.Optional[typing.Any] = OMIT,
        summary: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        collected_variables: typing.Optional[typing.Any] = OMIT,
        summary_pending: typing.Optional[bool] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        ended_at: typing.Optional[dt.datetime] = OMIT,
        end_reason: typing.Optional[str] = OMIT,
        disposition: typing.Optional[DispositionEnum] = OMIT,
        disposition_sentiment: typing.Optional[DispositionSentimentEnum] = OMIT,
        user_turn_count: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> VoiceSession:
        """
        Update the specified Voice Session.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        status : typing.Optional[Status9B1Enum]

        direction : typing.Optional[DirectionEnum]

        call_outcome : typing.Optional[CallOutcomeEnum]
            The telephony disposition.

            * `unknown` - Unknown
            * `answered` - Answered
            * `busy` - Busy
            * `no_answer` - No Answer
            * `failed` - Failed
            * `canceled` - Canceled
            * `voicemail` - Voicemail

        provider_ref_id : typing.Optional[str]
            The Call SID or external ID from the provider.

        from_number : typing.Optional[str]
            E.164 formatted number

        to_number : typing.Optional[str]
            E.164 formatted number

        duration_seconds : typing.Optional[int]
            Duration of the call in seconds

        recording_url : typing.Optional[str]
            Public or signed URL to the call audio.

        provider_status : typing.Optional[str]
            Raw status returned by the telephony provider.

        variables : typing.Optional[typing.Any]
            Campaign contact variables passed to the agent.

        summary : typing.Optional[str]
            LLM-generated one-sentence call summary.

        description : typing.Optional[str]
            LLM-generated 2-3 sentence call description.

        collected_variables : typing.Optional[typing.Any]
            LLM-extracted variables collected during the call (distinct from inbound campaign `variables`).

        summary_pending : typing.Optional[bool]
            True while the post-call summary pipeline owes this session a summary. Cleared on success or skip; the reconciler retries any row stuck True past the grace window.

        started_at : typing.Optional[dt.datetime]

        ended_at : typing.Optional[dt.datetime]

        end_reason : typing.Optional[str]

        disposition : typing.Optional[DispositionEnum]
            Computed interaction disposition category (ZEN-71).

            * `agent_escalation_immediate` - Agent Escalation Immediate
            * `agent_escalation_mid_call` - Agent Escalation Mid-Call
            * `hangup_immediate` - Hangup Immediate
            * `hangup_mid_call` - Hangup Mid-Call
            * `transfer_unsupported` - Transfer Unsupported
            * `resolved` - Resolved
            * `post_resolution_transfer` - Post Resolution Transfer
            * `fatal_error_escalated` - Fatal Error Escalated
            * `error` - Error

        disposition_sentiment : typing.Optional[DispositionSentimentEnum]
            Sentiment derived from the disposition (ZEN-71).

            * `positive` - Positive
            * `neutral` - Neutral
            * `negative` - Negative

        user_turn_count : typing.Optional[int]
            Number of user turns observed during the interaction. Reported by the runtime alongside disposition for analytics breakdowns (ZEN-71).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSession


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.sessions_partial_update(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.sessions_partial_update(
            id,
            status=status,
            direction=direction,
            call_outcome=call_outcome,
            provider_ref_id=provider_ref_id,
            from_number=from_number,
            to_number=to_number,
            duration_seconds=duration_seconds,
            recording_url=recording_url,
            provider_status=provider_status,
            variables=variables,
            summary=summary,
            description=description,
            collected_variables=collected_variables,
            summary_pending=summary_pending,
            started_at=started_at,
            ended_at=ended_at,
            end_reason=end_reason,
            disposition=disposition,
            disposition_sentiment=disposition_sentiment,
            user_turn_count=user_turn_count,
            request_options=request_options,
        )
        return _response.data

    async def sessions_confirm_upload_create(
        self, id: int, *, file_path: str, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceSession:
        """
        Confirm the recording upload and save the final S3 URL to the session

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        file_path : str
            The S3 file path of the uploaded recording

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSession


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.sessions_confirm_upload_create(
                id=1,
                file_path="file_path",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.sessions_confirm_upload_create(
            id, file_path=file_path, request_options=request_options
        )
        return _response.data

    async def sessions_recording_url_create(
        self,
        id: int,
        *,
        file_extension: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GenerateRecordingUrlResponse:
        """
        Generate a presigned URL for uploading voice recording to S3

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        file_extension : typing.Optional[str]
            File extension for the recording (default: wav)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GenerateRecordingUrlResponse


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.sessions_recording_url_create(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.sessions_recording_url_create(
            id, file_extension=file_extension, request_options=request_options
        )
        return _response.data

    async def sessions_status_partial_update(
        self,
        id: int,
        *,
        status: typing.Optional[Status9B1Enum] = OMIT,
        call_outcome: typing.Optional[CallOutcomeEnum] = OMIT,
        provider_status: typing.Optional[str] = OMIT,
        duration_seconds: typing.Optional[int] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        ended_at: typing.Optional[dt.datetime] = OMIT,
        end_reason: typing.Optional[str] = OMIT,
        disposition: typing.Optional[DispositionEnum] = OMIT,
        user_turn_count: typing.Optional[int] = OMIT,
        summary_pending: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> VoiceSession:
        """
        Update the call status and related fields throughout the call process

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        status : typing.Optional[Status9B1Enum]
            Session status

            * `created` - Created
            * `queued` - Queued
            * `active` - Active
            * `completed` - Completed
            * `failed` - Failed
            * `cancelled` - Cancelled

        call_outcome : typing.Optional[CallOutcomeEnum]
            Call outcome

            * `unknown` - Unknown
            * `answered` - Answered
            * `busy` - Busy
            * `no_answer` - No Answer
            * `failed` - Failed
            * `canceled` - Canceled
            * `voicemail` - Voicemail

        provider_status : typing.Optional[str]
            Raw status from provider

        duration_seconds : typing.Optional[int]
            Duration in seconds

        started_at : typing.Optional[dt.datetime]
            Start time

        ended_at : typing.Optional[dt.datetime]
            End time

        end_reason : typing.Optional[str]
            Reason for call ending

        disposition : typing.Optional[DispositionEnum]
            Computed call disposition category (ZEN-71). The matching disposition_sentiment is derived server-side — clients should not send it.

            * `agent_escalation_immediate` - Agent Escalation Immediate
            * `agent_escalation_mid_call` - Agent Escalation Mid-Call
            * `hangup_immediate` - Hangup Immediate
            * `hangup_mid_call` - Hangup Mid-Call
            * `transfer_unsupported` - Transfer Unsupported
            * `resolved` - Resolved
            * `post_resolution_transfer` - Post Resolution Transfer
            * `fatal_error_escalated` - Fatal Error Escalated
            * `error` - Error

        user_turn_count : typing.Optional[int]
            Number of user turns observed during the call (ZEN-71).

        summary_pending : typing.Optional[bool]
            Reliability marker for the post-call summary pipeline (architect H7). Voice-py sets True alongside the terminal status update — BEFORE the inline summary path starts — so an OOM-kill between the status update and the summary path still leaves a breadcrumb the reconciler can sweep. Cleared on successful summary persist.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSession


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.sessions_status_partial_update(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.sessions_status_partial_update(
            id,
            status=status,
            call_outcome=call_outcome,
            provider_status=provider_status,
            duration_seconds=duration_seconds,
            started_at=started_at,
            ended_at=ended_at,
            end_reason=end_reason,
            disposition=disposition,
            user_turn_count=user_turn_count,
            summary_pending=summary_pending,
            request_options=request_options,
        )
        return _response.data

    async def sessions_transcripts_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceSessionsTranscriptsRetrieveResponse:
        """
        Add a single transcript post to the session's conversation

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSessionsTranscriptsRetrieveResponse


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.sessions_transcripts_retrieve(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.sessions_transcripts_retrieve(id, request_options=request_options)
        return _response.data

    async def sessions_transcripts_create(
        self,
        id: int,
        *,
        text_src: TextSrcEnum,
        text: typing.Optional[str] = OMIT,
        timestamp: typing.Optional[dt.datetime] = OMIT,
        event_type: typing.Optional[EventTypeEnum] = OMIT,
        metadata: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> VoiceSessionsTranscriptsCreateResponse:
        """
        Add a single transcript post to the session's conversation

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        text_src : TextSrcEnum
            Source of the text (User, Assistant, Tool, Step, Variable). Use Step/Variable for rich events.

            * `User` - User
            * `Assistant` - Assistant
            * `Tool` - Tool
            * `Step` - Step
            * `Variable` - Variable

        text : typing.Optional[str]
            The transcript text content. Required for event_type='message'; optional for structured events where metadata carries the payload.

        timestamp : typing.Optional[dt.datetime]
            Timestamp for the transcript (optional, will be used as created_at)

        event_type : typing.Optional[EventTypeEnum]
            Discriminator for rich transcript events. Defaults to 'message' for backward compatibility.

            * `message` - Message
            * `step_enter` - Step Enter
            * `step_exit` - Step Exit
            * `step_transition` - Step Transition
            * `node_transition` - Node Transition
            * `variable_commit` - Variable Commit
            * `variable_rejected` - Variable Rejected
            * `run_tool_dispatched` - Run Tool Dispatched
            * `run_tool_completed` - Run Tool Completed

        metadata : typing.Optional[typing.Any]
            Structured payload for non-message events. Must be a JSON object.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSessionsTranscriptsCreateResponse


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.sessions_transcripts_create(
                id=1,
                text_src="User",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.sessions_transcripts_create(
            id,
            text_src=text_src,
            text=text,
            timestamp=timestamp,
            event_type=event_type,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def sessions_variables_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceSessionsVariablesRetrieveResponse:
        """
        Return extracted conversation variables for this session, excluding private variables

        Parameters
        ----------
        id : int
            A unique integer value identifying this Voice Session.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceSessionsVariablesRetrieveResponse


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.sessions_variables_retrieve(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.sessions_variables_retrieve(id, request_options=request_options)
        return _response.data

    async def sessions_terminal_by_workflow_create(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        ``POST /api/v1/voice/sessions/terminal-by-workflow/``

        Marks a ``VoiceInteractionSession`` terminal when no bot lifecycle
        will write it (AWS Connect no-answer/busy/failed paths). Looks up
        the session via the ZEN-204 ``OutboundCallCorrelation.session_id``
        bridge, atomically updates the parent + child tables under a
        WHERE-IN guard so already-active rows are no-ops.

        Tenant scoping: ``integration_workflow_id`` is globally unique, so
        the row's tenant is implicit (same pattern as the abandon endpoint).
        Service-token auth bypasses the RLS middleware; we set
        ``app.current_tenant_id`` GUC explicitly inside the transaction.

        Request body::

            {
                "integration_workflow_id": "<temporal-wf-id>",
                "call_outcome": "no_answer" | "busy" | "failed" | "canceled" | "answered" | ...,
                "duration_seconds": <int>,           # optional, default 0
                "raw_disconnect_reason": "<str>"     # optional, default ""
            }

        Responses (always 200 — best-effort writer; failures are diagnostic):
            ``{"updated": true,  "session_id": <int>, "reason": "updated"}``
            ``{"updated": false, "reason": "correlation_not_found"}``
            ``{"updated": false, "reason": "no_session_bound"}``  # pre-ZEN-204 voice-py
            ``{"updated": false, "session_id": <int>, "reason": "already_terminal"}``
            400 — validation error
            401 — invalid/missing service token

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.sessions_terminal_by_workflow_create()


        asyncio.run(main())
        """
        _response = await self._raw_client.sessions_terminal_by_workflow_create(request_options=request_options)
        return _response.data

    async def transcribers_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        workflow: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedTranscriberList:
        """
        List all Deepgram Transcribers matching the applied filters

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        workflow : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedTranscriberList


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.transcribers_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.transcribers_list(
            page=page, page_size=page_size, workflow=workflow, request_options=request_options
        )
        return _response.data

    async def transcribers_create(
        self, *, request: Transcriber, request_options: typing.Optional[RequestOptions] = None
    ) -> Transcriber:
        """
        Create a new Deepgram Transcriber.

        Parameters
        ----------
        request : Transcriber

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Transcriber


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient, Transcriber_DeepgramTranscriber

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.transcribers_create(
                request=Transcriber_DeepgramTranscriber(
                    workflow=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.transcribers_create(request=request, request_options=request_options)
        return _response.data

    async def transcribers_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> Transcriber:
        """
        Retrieve the specified Deepgram Transcriber.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Transcriber.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Transcriber


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.transcribers_retrieve(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.transcribers_retrieve(id, request_options=request_options)
        return _response.data

    async def transcribers_update(
        self, id: int, *, request: Transcriber, request_options: typing.Optional[RequestOptions] = None
    ) -> Transcriber:
        """
        Update the specified Deepgram Transcriber.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Transcriber.

        request : Transcriber

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Transcriber


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient, Transcriber_DeepgramTranscriber

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.transcribers_update(
                id=1,
                request=Transcriber_DeepgramTranscriber(
                    workflow=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.transcribers_update(id, request=request, request_options=request_options)
        return _response.data

    async def transcribers_destroy(self, id: int, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete the specified Deepgram Transcriber. This may archive/delete related objects.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Transcriber.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.transcribers_destroy(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.transcribers_destroy(id, request_options=request_options)
        return _response.data

    async def transcribers_partial_update(
        self, id: int, *, request: PatchedTranscriber, request_options: typing.Optional[RequestOptions] = None
    ) -> Transcriber:
        """
        Update the specified Deepgram Transcriber.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Transcriber.

        request : PatchedTranscriber

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Transcriber


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient, PatchedTranscriber_DeepgramTranscriber

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.transcribers_partial_update(
                id=1,
                request=PatchedTranscriber_DeepgramTranscriber(),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.transcribers_partial_update(
            id, request=request, request_options=request_options
        )
        return _response.data

    async def transcribers_switch_resourcetype_create(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Replace a transcriber row with a different child type.

        The ``workflow`` OneToOneField unique constraint blocks an
        insert-then-delete approach, so we hard-delete the old row inside
        an atomic block and recreate via the polymorphic serializer.
        ``resourcetype`` in the payload picks the new child type; the rest
        of the body is forwarded to the serializer for validation.

        Accepted ``resourcetype`` values come from
        ``TranscriberPolymorphicSerializer.valid_resourcetypes()``, derived
        from ``model_serializer_mapping`` — adding a new child type only
        requires a mapping entry.

        Field-carryover semantics: any field omitted from the payload would
        otherwise reset to the model default on the freshly inserted row.
        For workspace-secret links (``api_key_secret``) and the human
        ``title`` we explicitly carry the value over from the old row when
        the payload omits the key — so callers (UI or API) don't silently
        drop the secret link or the user-given name on a type switch.
        Explicit ``"api_key_secret": null`` / ``"title": null`` in the body
        still clear them (we use key-presence, not truthiness, so explicit-
        null is honored).

        Errors during serializer validation propagate as
        ``ValidationError`` (400) and Django auto-rolls back the txn.

        .. note::
            ``QuerySet.delete()`` fires ``pre_delete``/``post_delete`` signals on
            the old row. Receivers that perform external side effects (webhooks,
            cleanup tasks) will trigger spuriously on every type switch. If you
            add such a receiver, gate it on ``not getattr(instance, "_swapping",
            False)`` or similar.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Transcriber.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.transcribers_switch_resourcetype_create(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.transcribers_switch_resourcetype_create(id, request_options=request_options)
        return _response.data

    async def voice_providers_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        workflow: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedVoiceProviderList:
        """
        List all Cartesia Voice Providers matching the applied filters

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        workflow : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedVoiceProviderList


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.voice_providers_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.voice_providers_list(
            page=page, page_size=page_size, workflow=workflow, request_options=request_options
        )
        return _response.data

    async def voice_providers_create(
        self, *, request: VoiceProvider, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceProvider:
        """
        Create a new Cartesia Voice Provider.

        Parameters
        ----------
        request : VoiceProvider

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceProvider


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient, VoiceProvider_CartesiaVoiceProvider

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.voice_providers_create(
                request=VoiceProvider_CartesiaVoiceProvider(
                    workflow=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.voice_providers_create(request=request, request_options=request_options)
        return _response.data

    async def voice_providers_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceProvider:
        """
        Retrieve the specified Cartesia Voice Provider.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Voice Provider.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceProvider


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.voice_providers_retrieve(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.voice_providers_retrieve(id, request_options=request_options)
        return _response.data

    async def voice_providers_update(
        self, id: int, *, request: VoiceProvider, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceProvider:
        """
        Update the specified Cartesia Voice Provider.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Voice Provider.

        request : VoiceProvider

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceProvider


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient, VoiceProvider_CartesiaVoiceProvider

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.voice_providers_update(
                id=1,
                request=VoiceProvider_CartesiaVoiceProvider(
                    workflow=1,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.voice_providers_update(id, request=request, request_options=request_options)
        return _response.data

    async def voice_providers_destroy(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Delete the specified Cartesia Voice Provider. This may archive/delete related objects.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Voice Provider.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.voice_providers_destroy(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.voice_providers_destroy(id, request_options=request_options)
        return _response.data

    async def voice_providers_partial_update(
        self, id: int, *, request: PatchedVoiceProvider, request_options: typing.Optional[RequestOptions] = None
    ) -> VoiceProvider:
        """
        Update the specified Cartesia Voice Provider.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Voice Provider.

        request : PatchedVoiceProvider

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoiceProvider


        Examples
        --------
        import asyncio

        from zenlabs_sdk import (
            AsyncZenClient,
            PatchedVoiceProvider_CartesiaVoiceProvider,
        )

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.voice_providers_partial_update(
                id=1,
                request=PatchedVoiceProvider_CartesiaVoiceProvider(),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.voice_providers_partial_update(
            id, request=request, request_options=request_options
        )
        return _response.data

    async def voice_providers_switch_resourcetype_create(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Replace a voice provider row with a different child type.

        The ``workflow`` OneToOneField unique constraint blocks an
        insert-then-delete approach, so we hard-delete the old row inside
        an atomic block and recreate via the polymorphic serializer.
        ``resourcetype`` in the payload picks the new child type; the rest
        of the body is forwarded to the serializer for validation.

        Accepted ``resourcetype`` values come from
        ``VoiceProviderPolymorphicSerializer.valid_resourcetypes()``, derived
        from ``model_serializer_mapping`` — adding a new child type only
        requires a mapping entry.

        Field-carryover semantics: any field omitted from the payload would
        otherwise reset to the model default on the freshly inserted row.
        For workspace-secret links (``api_key_secret``) and the human
        ``title`` we explicitly carry the value over from the old row when
        the payload omits the key — so callers (UI or API) don't silently
        drop the secret link or the user-given name on a type switch.
        Explicit ``"api_key_secret": null`` / ``"title": null`` in the body
        still clear them (we use key-presence, not truthiness, so explicit-
        null is honored).

        Errors during serializer validation propagate as
        ``ValidationError`` (400) and Django auto-rolls back the txn.

        .. note::
            ``QuerySet.delete()`` fires ``pre_delete``/``post_delete`` signals on
            the old row. Receivers that perform external side effects (webhooks,
            cleanup tasks) will trigger spuriously on every type switch. If you
            add such a receiver, gate it on ``not getattr(instance, "_swapping",
            False)`` or similar.

        Parameters
        ----------
        id : int
            A unique integer value identifying this Base Voice Provider.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.voice_providers_switch_resourcetype_create(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.voice_providers_switch_resourcetype_create(
            id, request_options=request_options
        )
        return _response.data

    async def workflow_config_by_id_retrieve(
        self, workflow_id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> WorkflowConfiguration:
        """
        Fetch workflow, nodes, edges, and provider config for a specific workflow ID.

        Parameters
        ----------
        workflow_id : int
            The workflow ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WorkflowConfiguration


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.workflow_config_by_id_retrieve(
                workflow_id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.workflow_config_by_id_retrieve(workflow_id, request_options=request_options)
        return _response.data

    async def workflow_config_retrieve(
        self,
        phone_number: str,
        *,
        direction: VoiceWorkflowConfigRetrieveRequestDirection,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> WorkflowConfiguration:
        """
        Fetch workflow, nodes, edges, and provider config for a specific phone number.

        Parameters
        ----------
        phone_number : str
            The phone number (E.164 format, e.g., +15550001111)

        direction : VoiceWorkflowConfigRetrieveRequestDirection
            Call direction ('inbound' or 'outbound')

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WorkflowConfiguration


        Examples
        --------
        import asyncio

        from zenlabs_sdk import AsyncZenClient

        client = AsyncZenClient(
            tenant_id="YOUR_TENANT_ID",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.voice.workflow_config_retrieve(
                phone_number="phone_number",
                direction="inbound",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.workflow_config_retrieve(
            phone_number, direction=direction, request_options=request_options
        )
        return _response.data
