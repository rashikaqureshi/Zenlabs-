# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .user import User


class FatalErrorEscalationConfig(UniversalBaseModel):
    """
    Bot-level fallback escalation config (ZEN-56).

    Distinct from ``EscalationConfigSerializer`` (ZEN-53). This config is
    consumed by voice-py's FatalErrorWatcher processor to determine the
    fallback destination when STT/LLM/TTS services fail catastrophically.
    """

    id: typing.Optional[int] = None
    enabled: typing.Optional[bool] = pydantic.Field(default=None)
    """
    Enable fallback redirect on fatal STT/LLM/TTS errors.
    """

    fallback_transfer_number: typing.Optional[int] = pydantic.Field(default=None)
    """
    Reference to a centrally-managed transfer number used as the fallback destination when a fatal system error is detected. PROTECT prevents removing a TransferNumber that is wired as a safety net — admin must disable or reassign the config first.
    """

    speak_error_message: typing.Optional[bool] = pydantic.Field(default=None)
    """
    If True, the voice agent speaks the error_message (or a default apology) before redirecting. If False, the redirect is silent — no TTS is played at all. Useful when the TTS provider is the failing service or when customers prefer a clean handoff.
    """

    error_message: typing.Optional[str] = pydantic.Field(default=None)
    """
    Message spoken to the caller before the fallback transfer. If empty, a default apology is used. Ignored when speak_error_message is False.
    """

    respect_business_hours: typing.Optional[bool] = pydantic.Field(default=None)
    """
    If True, out-of-hours fatal errors play the OOH message and hang up (matches ZEN-54 semantics). If False, the fallback transfer fires regardless of business hours.
    """

    created_by: typing.Optional[User] = None
    created_at: typing.Optional[dt.datetime] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
