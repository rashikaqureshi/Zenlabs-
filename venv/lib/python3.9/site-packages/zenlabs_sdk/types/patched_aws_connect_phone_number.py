# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .environment_enum import EnvironmentEnum


class PatchedAwsConnectPhoneNumber(UniversalBaseModel):
    """
    Mixin that enforces model-level validate_unique() on create/update.

    DRF's ModelSerializer does NOT call validate_unique() by default.
    This mixin plugs that gap by building a temporary instance to run
    validation, then delegating the actual save to DRF's normal flow
    (which handles tenant, created_by, etc.).

    Apply to the *child* model serializers (e.g. BaseVariableSerializer),
    NOT to the PolymorphicSerializer wrapper.

    Must appear before ModelSerializer in MRO so its create/update
    take precedence.
    """

    id: typing.Optional[int] = None
    title: typing.Optional[str] = pydantic.Field(default=None)
    """
    Title for the phone number
    """

    phone_number: typing.Optional[str] = pydantic.Field(default=None)
    """
    E.164 formatted number
    """

    environment: typing.Optional[EnvironmentEnum] = pydantic.Field(default=None)
    """
    Is this number used for testing or live traffic?
    
    * `sandbox` - Sandbox
    * `production` - Production
    """

    inbound_workflow: typing.Optional[int] = pydantic.Field(default=None)
    """
    Workflow triggered when this number receives an inbound call
    """

    outbound_workflow: typing.Optional[int] = pydantic.Field(default=None)
    """
    Workflow triggered when this number makes an outbound call
    """

    aws_instance_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Amazon Connect instance UUID (from ListInstances).
    """

    aws_region: typing.Optional[str] = pydantic.Field(default=None)
    """
    AWS region the Connect instance lives in (e.g. us-east-1).
    """

    aws_contact_flow_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Default contact flow used for outbound dials unless the workflow YAML overrides via telephony_config.aws_contact_flow_id.
    """

    aws_phone_arn: typing.Optional[str] = pydantic.Field(default=None)
    """
    Resolved via Connect ``ListPhoneNumbersV2`` at import time — the canonical identifier used as the SOURCE number on outbound ``StartOutboundVoiceContact`` calls.
    """

    bridge_phone: typing.Optional[str] = pydantic.Field(default=None)
    """
    BYOC bridge number — the Twilio E.164 number that AWS Connect's outbound SIP leg lands on (i.e. what arrives as ``To`` on the Twilio voice webhook). Distinct from ``phone_number`` (the AWS caller-ID). Used as the ``source_phone`` discriminator on the outbound correlation row so the inbound webhook claim matches. Leave blank when AWS caller-ID equals the Twilio bridge number; the dispatch layer falls back to ``phone_number`` in that case.
    """

    integration_connection: typing.Optional[int] = pydantic.Field(default=None)
    """
    Nango-managed aws-iam connection. Credentials never touch this row — the SigV4 helper in voice-py pulls them fresh from Nango on each dial.
    """

    webhook_sync_status: typing.Optional[str] = pydantic.Field(default=None)
    """
    Always 'n/a' for Amazon Connect — Twilio-style webhook sync does not apply since calls route via Connect contact flows.
    """

    created_at: typing.Optional[dt.datetime] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
