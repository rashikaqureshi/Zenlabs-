# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .edge_condition_group import EdgeConditionGroup
from .user import User


class Edge(UniversalBaseModel):
    """
    Validates ``{{uuid:Variable:...}}`` references in template text fields.

    Subclasses must define ``_uuid_ref_fields``: a tuple of field names
    to validate (e.g. ``("content",)`` or ``("goodbye_message", "prompt")``).

    Scope checking is performed when the node can be resolved — referenced
    variables must be accessible from the node that owns the text field.
    On CREATE (no instance), existence checking still runs if ``workflow_id``
    can be resolved from the view's URL kwargs.
    """

    id: typing.Optional[int] = None
    workflow: typing.Optional[int] = pydantic.Field(default=None)
    """
    The workflow that this edge belongs to
    """

    from_node: int = pydantic.Field()
    """
    The node that this edge starts from
    """

    to_node: int = pydantic.Field()
    """
    The node that this edge ends at
    """

    to_node_title: typing.Optional[str] = None
    meta: typing.Optional[typing.Any] = pydantic.Field(default=None)
    """
    Edge metadata including conditional descriptions
    """

    order: typing.Optional[int] = pydantic.Field(default=None)
    """
    Evaluation order for rule-based outcomes (lower = earlier). Ignored for prompt-based outcomes.
    """

    is_else: typing.Optional[bool] = pydantic.Field(default=None)
    """
    If True, this is the default/fallback edge (conditions are ignored). Required for rule-based outcomes.
    """

    is_resolution_driving: typing.Optional[bool] = pydantic.Field(default=None)
    """
    If True, picking this edge when the source LLMNode transitions marks the call as having a customer-intent-fulfilled moment. Default True. Opt OUT for escalation / abandonment / wrong-number edges.
    """

    condition_groups: typing.Optional[typing.List[EdgeConditionGroup]] = None
    created_by: typing.Optional[User] = None
    created_at: typing.Optional[dt.datetime] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
