# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .deepgram_flux_transcriber_model_slug_enum import DeepgramFluxTranscriberModelSlugEnum


class DeepgramFluxTranscriber(UniversalBaseModel):
    """
    Mask the ``api_key`` field on read and ignore the sentinel on write.

    Serializers that include a credential-shaped ``api_key`` field mix this
    in to get two guarantees:

    * Default-deny on read: ``api_key`` is replaced with ``"****"`` unless
      the caller explicitly opts in via ``context["include_secrets"]=True``.
      Empty/null stored values pass through as ``None`` so consumers can
      still distinguish "no key configured" from "key is hidden".
    * Round-trip safety on write: if a payload submits ``api_key="****"``
      (the masked sentinel a UI just GET'd), the field is dropped before
      reaching the model so the stored credential is preserved. Any other
      string value writes through normally.
    """

    id: typing.Optional[int] = None
    title: typing.Optional[str] = pydantic.Field(default=None)
    """
    Title for the transcriber
    """

    workflow: int = pydantic.Field()
    """
    The workflow that this transcriber belongs to
    """

    api_key: typing.Optional[str] = pydantic.Field(default=None)
    """
    API key for the transcriber
    """

    api_key_secret: typing.Optional[int] = pydantic.Field(default=None)
    """
    Optional workspace secret holding the transcriber API key. Takes precedence over the ``api_key`` column when set.
    """

    api_key_secret_name: typing.Optional[str] = None
    model_slug: typing.Optional[DeepgramFluxTranscriberModelSlugEnum] = None
    eager_eot_threshold: typing.Optional[float] = pydantic.Field(default=None)
    """
    Eager End-of-Turn threshold, EagerEndOfTurn/TurnResumed are off by default. You can turn them on by setting eager_eot_threshold to a valid value.
                    Lower values = more aggressive EagerEndOfTurning (faster response, more LLM calls).
                    Higher values = more conservative EagerEndOfTurning (slower response, fewer LLM calls).
    """

    eot_threshold: typing.Optional[float] = pydantic.Field(default=None)
    """
    End-of-Turn threshold, End-of-turn confidence required to finish a turn (default 0.7).
                    Lower values = turns end sooner (more interruptions, faster responses).
                    Higher values = turns end later (fewer interruptions, more complete utterances).
    """

    eot_timeout_ms: typing.Optional[int] = pydantic.Field(default=None)
    """
    End-of-Turn timeout in milliseconds, Time in milliseconds after speech to finish a turn
                    regardless of EOT confidence (default 5000)
    """

    keyterm: typing.Optional[typing.List[str]] = pydantic.Field(default=None)
    """
    List of keyterms to boost recognition accuracy for specialized terminology
    """

    mip_opt_out: typing.Optional[bool] = pydantic.Field(default=None)
    """
    Opts out requests from the Deepgram Model Improvement Program
    """

    tag: typing.Optional[typing.List[str]] = pydantic.Field(default=None)
    """
    List of tags to label requests for identification during usage reporting
    """

    min_confidence: typing.Optional[float] = pydantic.Field(default=None)
    """
    Minimum confidence for the transcriber
    """

    ttfs_p99latency: typing_extensions.Annotated[
        typing.Optional[float],
        FieldMetadata(alias="ttfs_p99_latency"),
        pydantic.Field(
            alias="ttfs_p99_latency",
            description="P99 latency from speech end to final transcript in seconds, default is 0.35 seconds",
        ),
    ] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
